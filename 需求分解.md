# 数据中心系统需求分解

> 版本：v0.2（已纳入全部待确认问题答复，RBAC 权限模型已修正）
> 技术栈：Go + Gin + MongoDB
> 运行环境：Slurm 计算集群（本阶段不涉及）、NFS 共享存储（所有节点挂载同一套文件）、数千节点
> 数据特征：计算光刻测试数据（model、版图、recipe 等），数据量大

---

## 1. 背景与目标

在共享 NFS 存储的集群上构建一个数据中心系统，核心职责：

1. **数据的存储与查找**：管理文件/文件夹的元数据与标签，支持按标签查询；
2. **集合管理**：以「集合」为单位组织数据，集合可自定义标签体系与刮削脚本；
3. **刮削**：通过集合注册的刮削脚本（NFS 上的脚本路径），从文件/文件夹中提取标签并入库。

本阶段需要实现的内容：

| # | 需求项 | 说明 |
|---|---|---|
| R1 | 基于 RBAC 的权限与用户管理 | 系统管理员、集合管理员、集合操作工 |
| R2 | 基于集合管理各模块数据 | admin 创建集合；集合管理员管理集合（自定义标签：名称/类型/必填/非必填），授权操作工；操作工对有权限集合的数据增删改查，但**不能修改集合标签定义** |
| R3 | 刮削系统 | 调用集合注册的刮削脚本刮出文件/文件夹的标签并保存数据；**每个集合有且仅有一个刮削脚本**（以路径注册）；**支持多个数据并发刮削** |

---

## 2. 核心概念与术语

> 为避免混淆：下文中「业务集合」指本系统业务上的 Collection；「MongoDB 集合」专指 MongoDB 中的 collection 存储单元。

| 术语 | 说明 |
|---|---|
| 业务集合（Collection） | 数据的组织单元，由 admin 创建；拥有自己的标签定义、唯一刮削脚本、成员列表 |
| 标签定义（Tag Definition） | 集合上声明的标签规则：标签名、值类型、是否必填 |
| 标签值（Tag Value） | 数据项上实际的标签键值对，受集合标签定义约束 |
| 数据项（Data Item） | 一个文件或文件夹（NFS 上的路径）+ 其标签值；隶属于某个业务集合 |
| 直接添加 | 添加数据项时只登记路径与初始标签，不触发刮削 |
| 刮削添加 | 添加数据项时登记路径后立即自动触发刮削，由刮削产出标签 |
| 刮削脚本（Scrape Script） | 集合唯一的、以 NFS 路径注册的可执行脚本；输入一个路径，输出 JSON 标签 |
| 刮削任务（Scrape Task） | 对某个数据项执行刮削脚本的一次任务记录 |

---

## 3. 系统总体架构

```
┌──────────────┐        ┌──────────────────────────┐
│   客户端      │  HTTP  │      Gin 后端服务          │
│ (Web/API)    │───────▶│  ├─ 认证/用户/权限 (RBAC)  │
└──────────────┘        │  ├─ 集合管理              │
                        │  ├─ 标签定义管理          │
                        │  ├─ 数据项 CRUD + 查询    │
                        │  └─ 刮削任务管理接口      │
                        └───────┬──────────┬───────┘
                                │          │
                    ┌───────────▼──┐   ┌───▼──────────────┐
                    │   MongoDB    │   │   刮削系统(子系统) │
                    │ 元数据/标签/  │   │  ├─ 任务队列      │
                    │ 权限/任务记录 │   │  ├─ Worker 池     │
                    └──────────────┘   │  └─ 脚本执行器    │
                                       └───┬──────────────┘
                                           │ 执行脚本(位于 NFS)
                                ┌──────────▼──────────────┐
                                │   NFS 共享存储           │
                                │  ├─ 数据文件/文件夹      │
                                │  └─ 刮削脚本            │
                                └─────────────────────────┘
```

**关键边界**：

- **文件本体只存 NFS**，MongoDB 只存元数据（路径、标签、状态、权限、任务记录）；
- **刮削系统是独立子系统**：可独立部署/独立扩缩容，与 Gin 服务通过共享 MongoDB 和任务队列解耦（详见 §5.5、§8）；
- 因为所有节点共享 NFS，**路径在任何节点上语义一致**，刮削 worker 可以跑在任意节点上。

---

## 4. RBAC 权限模型（R1）

### 4.1 角色定义

角色分为**全局角色**与**集合级角色**两类：

| 层级 | 角色 | 来源 | 说明 |
|---|---|---|---|
| 全局 | 系统管理员 `admin` | users.role | 用户管理、创建集合、删除集合、指定/更换集合管理员 |
| 集合级 | 集合管理员 `collection_admin` | 集合 members 列表 | 管理**自己管理的集合**（标签定义、刮削脚本、成员授权），对本集合数据有完整操作权 |
| 集合级 | 集合操作工 `operator` | 集合 members 列表 | 对**被授权的集合**做数据项增删改查；**不能修改集合标签定义** |
| — | 普通用户 `user` | users.role | 默认无任何集合权限，等待被加入集合 |

> **核心原则**：权限 = **全局角色（公共模块权限）** + **集合级授权（以集合为单位独立定义）**。
> 公共模块（用户管理、创建/删除集合等）的权限按全局角色总体定义；
> 集合相关的一切权限，**每个集合单独定义**（存储在该集合文档的 `members` 数组中），一个用户在集合 A 的角色不影响其在集合 B 的权限。

### 4.2 权限点清单

**① 公共权限点（全局判定，不依赖具体集合，与集合无绑定关系）**

| 权限点 | 含义 | 判定方式 |
|---|---|---|
| `user:manage` | 创建/修改/禁用/删除用户 | 全局角色 = admin |
| `collection:create` | 创建业务集合 | 全局角色 = admin |
| `collection:delete` | 删除业务集合（级联删除元数据） | 全局角色 = admin |
| `collection:assign_admin` | 创建时指定集合管理员、更换集合管理员 | 全局角色 = admin |
| `collection:list_all` | 查看所有集合列表 | 全局角色 = admin |

**② 集合权限点（必须绑定具体集合判定：`权限点 × collection_id`）**

> 判定方式：查询**该集合**的 `members`，取得当前用户**在该集合中的角色**，再对照下表。
> 同一用户在不同集合中的角色互不影响；集合管理员只对自己管理的集合有效，**不因拥有某个集合级角色而获得其他集合的任何权限**。

| 权限点 | 含义 | 集合管理员（本集合） | 操作工（本集合） |
|---|---|---|---|
| `collection:read` | 查看集合信息与标签定义 | ✅ | ✅ |
| `collection:update` | 修改集合基础信息 | ✅ | ❌ |
| `tag_schema:update` | 增删改标签定义 | ✅ | ❌ |
| `script:update` | 设置/修改刮削脚本路径 | ✅ | ❌ |
| `member:grant` | 授权/撤销本集合操作工 | ✅ | ❌ |
| `item:create` | 添加数据项（直接添加/刮削添加） | ✅ | ✅ |
| `item:read` | 查看数据项、按标签查询 | ✅ | ✅ |
| `item:update` | 修改数据项（标签值、路径） | ✅ | ✅ |
| `item:delete` | 删除数据项（仅元数据） | ✅ | ✅ |
| `item:scrape` | 触发手动刮削 | ✅ | ✅ |

### 4.3 权限矩阵汇总

**公共权限（按全局角色）**

| 操作 | admin | 普通用户 |
|---|---|---|
| 用户管理 | ✅ | ❌ |
| 创建集合 | ✅ | ❌ |
| 删除集合 | ✅ | ❌ |
| 指定/更换集合管理员 | ✅ | ❌ |
| 查看全部集合列表 | ✅ | ❌ |

**集合权限（按「该集合的 members」逐集合判定；全局 admin 例外：全权放行）**

| 操作 | 集合管理员（本集合） | 操作工（本集合） | 非本集合成员（admin 除外：查看/修改全部放行） |
|---|---|---|---|
| 查看集合信息/标签定义 | ✅ | ✅ | ❌（admin ✅） |
| 修改集合基础信息 | ✅ | ❌ | ❌（admin ✅） |
| 管理标签定义 | ✅ | ❌ | ❌（admin ✅） |
| 管理刮削脚本路径 | ✅ | ❌ | ❌（admin ✅） |
| 授权/撤销操作工 | ✅ | ❌ | ❌（admin ✅） |
| 数据项增删改查 | ✅ | ✅ | ❌（admin ✅） |
| 按标签查询 | ✅ | ✅ | ❌（admin ✅） |
| 手动触发刮削 | ✅ | ✅ | ❌（admin ✅） |

> 说明：**admin 拥有最高权限**，对任意集合的查看/修改全部放行（v0.12，等同于各集合的集合管理员 +
> 操作工），无需加入 `members`；非 admin 用户严格按该集合 `members` 逐集合判定。
> 集合的创建/删除/更换集合管理员仍为 admin 专属公共权限。
>
> **前端按钮渲染规则（与后端判定对齐，v0.14 排查确认）**：全部权限判断收敛为三类——
> ① `App.isAdmin()`：admin 专属操作（权限管理导航、新建/删除集合、更换集合管理员、路由守卫）；
> ② `isColAdmin`：集合管理操作（编辑描述/标签定义/刮削脚本/成员/删除策略），admin 恒为 true；
> ③ `canOperate`：数据操作（数据项编辑/手动刮削/添加与删除关联），admin 恒为 true。
> 任何新增按钮/操作必须复用这三类判断，避免再次出现"后端放行、前端隐藏"的权限断裂。

### 4.4 鉴权流程

1. 登录换取 JWT（用户身份 + 全局角色）；
2. 中间件校验 JWT，注入 `currentUser`；
3. **公共权限**：请求属于公共模块 → 按 `users.role` 判定公共权限点；
4. **集合权限**：请求携带 `collection_id` → 加载**该集合**的 `members`，取得当前用户在该集合的角色 → 判定 `(权限点, collection_id)`；
5. 未认证 → 401；认证但无权限 → 403。

---

## 5. 功能需求分解

### 5.1 用户与认证模块

- 用户由 **admin 创建**（不支持开放注册）；密码安全存储（bcrypt）；
- 登录（JWT）、登出、查看当前用户信息；
- admin 对用户的增删改查、启用/禁用、角色设置。

### 5.2 集合管理模块

- **admin 创建集合**：名称（**全局唯一**）、描述、初始集合管理员；
- **admin**：更换集合管理员、删除集合（**级联删除该集合的数据项与刮削任务元数据，不触碰 NFS 文件**）；
- **集合管理员（仅限自己管理的集合）**：
  - 修改集合基础信息；
  - 管理标签定义（§5.3）；
  - 管理刮削脚本路径（§5.5）；
  - 授权/撤销操作工（添加/移除 `members` 中 role=operator 的成员）；
- 集合列表：admin 看全部；其他用户只看自己参与的集合。

### 5.3 标签定义模块

- 标签定义字段：`名称`、`类型`、`是否必填`，类型相关的附加约束见下表：

| 类型 | 附加约束 | 校验规则 |
|---|---|---|
| `string` | — | 值必须为字符串 |
| `int` | — | 值必须为整数 |
| `float` | — | 值必须为数值 |
| `bool` | — | 值必须为布尔 |
| `date` | — | 值必须为日期/时间（建议 ISO 字符串） |
| `enum` | `enum_values: []` | 值必须为枚举值之一 |
| `array` | `element_type`（基础类型） | 值为数组，且元素类型符合 `element_type` |
| `object` | `fields: []`（嵌套子字段定义：名称/类型/必填，可递归） | 值为对象，递归校验子字段 |

- 标签名在集合内唯一，且**不得与系统保留字段冲突**（`id`、`path`、`collection_id`、`tags`、`created_at`、`updated_at` 等）；
- 标签数量**无上限**；
- 操作：
  - 新增标签（必填/非必填，可设默认值）；
  - 删除标签；
  - 修改类型 / 修改必填性；
- **修改标签定义对已有数据的影响（Q8，采用初稿策略）**：
  - 新增非必填标签：立即生效，旧数据无该标签；
  - 新增必填标签：旧数据标记为「标签不完整」，可重新刮削补齐；
  - 删除标签：从已有数据项中移除该标签；
  - 类型/必填性变更：做兼容性校验（不兼容则拒绝并提示，或标记脏数据待重刮）。

### 5.4 数据项模块（操作工核心操作面）

- **增（两种动作，均需校验路径存在性）**：
  1. **直接添加**：`path + 初始标签（可选）`，不触发刮削；
  2. **刮削添加**：`path`，创建数据项后自动创建刮削任务，标签由刮削产出；
  - 路径为 NFS 上的文件或文件夹，**后端只校验其存在性**（文件/文件夹均可），是否可刮削由脚本自行决定；
  - 路径必须位于允许的数据根目录内（防路径逃逸，见 §9 安全）。
- **删**：**仅删除数据库中的元数据，不删除 NFS 上的文件/文件夹本身**；
- **改**：
  - 标签值可修改（校验必填、类型）；
  - **路径可修改**（修改时校验新路径存在性；改路径后旧标签可能已不匹配，需手动重刮，见 Q2）；
- **查**：按标签条件查询（§5.6）；
- 权限：仅限被授权集合；操作工不能触碰集合的标签定义与刮削脚本。

### 5.5 刮削系统模块（R3）

**脚本管理（由集合管理员负责，仅限自己管理的集合）**

- 集合创建后，集合管理员**注册刮削脚本路径**（NFS 上的绝对路径）；
- **每个集合有且仅有一个刮削脚本**：再次注册即覆盖，历史任务保留旧路径快照；
- 注册时校验：路径存在、文件可读/可执行。

**刮削任务**

- 触发方式（Q2）：
  - **刮削添加时自动触发**（新增数据填入路径即自动刮削）；
  - **手动重刮**：对已有数据项手动触发；
  - **不支持定时重刮**；
- 任务输入：`集合的刮削脚本路径` + `数据项路径`（**仅一个入参**）；
- 任务输出：解析脚本产出的 JSON → **按集合标签定义校验**（必填、类型，含 enum/array/object 递归校验）→ 写入数据项 `tags` → 更新刮削状态；
- **并发**：支持多个数据项同时刮削（任务队列 + Worker 池，见 §8）；
- 任务状态机：`pending → running → success | failed`；失败可手动重试；
- 任务记录留存（审计 + 排查），包含脚本路径快照、数据路径、错误信息。

**任务生命周期**

```
添加数据项(刮削添加) / 手动触发 ──▶ 创建刮削任务(pending) ──▶ worker 领取(running)
                                                               │
                        ┌──────────────────────────────────────┤
             产出合法 JSON 且通过标签校验                    无合法 JSON / 超时 / 校验失败
                        ▼                                      ▼
                   写入标签(success)                      记录错误(failed) ──▶ 手动重试
```

### 5.6 查询模块

- 在集合范围内按标签查询：`?tag_name=value` 组合条件；
- 操作符（Q6 已确认）：等值、范围（`gt/gte/lt/lte`）、存在性（`exists`）、`in`；**string 不支持模糊/正则**；
- 必须分页（`page` / `pageSize`），默认排序按创建时间倒序；
- 权限过滤：只能查询自己参与的集合（`(item:read, collection_id)` 判定）。

---

## 6. 数据模型设计（MongoDB）

### 6.1 存储策略讨论

- **方案 A（已确认采用）**：所有业务集合的数据项统一存到一个 MongoDB 集合 `data_items`，以 `collection_id` 区分。优点：MongoDB 集合数量可控、动态标签直接内嵌在 `tags` 子文档（MongoDB 天然支持灵活 schema）。
- 方案 B：每个业务集合对应一个独立 MongoDB 集合。缺点：业务集合数量多时管理成本高、索引难以统一治理。
- 数据量级（Q7）：**单集合约 10 万数据项** → 单机 MongoDB 完全可承载，暂无需分片；未来需要时可按 `collection_id` 做分片键平滑扩展。
- 刮削 TPS 不可预估（python 脚本可能长时间运行）→ 队列设计必须容忍长任务（见 §8.2）。

### 6.2 集合结构示例

**users**

```json
{
  "_id": "ObjectId",
  "username": "alice",
  "password_hash": "bcrypt...",
  "role": "admin | user",
  "status": "active | disabled",
  "created_at": "ISODate"
}
```

**business_collections**

```json
{
  "_id": "ObjectId",
  "name": "光刻模型库",
  "description": "用于计算光刻的 model 数据",
  "created_by": "user_id",
  "created_at": "ISODate",
  "updated_at": "ISODate",
  "tag_schema": [
    { "name": "model_name", "type": "string", "required": true },
    { "name": "age",        "type": "int",    "required": false },
    { "name": "stage",      "type": "enum",   "enum_values": ["dev", "test", "prod"], "required": true },
    { "name": "keywords",   "type": "array",  "element_type": "string", "required": false },
    { "name": "config",     "type": "object", "fields": [
        { "name": "version",  "type": "string", "required": true },
        { "name": "accuracy", "type": "float",  "required": false }
      ], "required": false }
  ],
  "scrape_script": {
    "path": "/nfs/scripts/model_scraper.sh",
    "updated_by": "user_id",
    "updated_at": "ISODate"
  },
  "members": [
    { "user_id": "u2", "role": "collection_admin" },
    { "user_id": "u3", "role": "operator" }
  ]
}
```

**data_items**

```json
{
  "_id": "ObjectId",
  "collection_id": "ObjectId",
  "path": "/nfs/data/models/xxx/v1",
  "tags": {
    "model_name": "abc",
    "age": 3,
    "stage": "test",
    "keywords": ["opc", "model"],
    "config": { "version": "1.2", "accuracy": 0.98 }
  },
  "tag_source": "manual | scrape | mixed",
  "scrape_status": "pending | success | failed | none",
  "last_scraped_at": "ISODate",
  "created_by": "user_id",
  "created_at": "ISODate",
  "updated_at": "ISODate"
}
```

**scrape_tasks**

```json
{
  "_id": "ObjectId",
  "collection_id": "ObjectId",
  "item_id": "ObjectId",
  "script_path": "/nfs/scripts/model_scraper.sh",
  "data_path": "/nfs/data/models/xxx/v1",
  "status": "pending | running | success | failed",
  "trigger_by": "user_id | auto",
  "created_at": "ISODate",
  "started_at": "ISODate",
  "finished_at": "ISODate",
  "exit_code": 0,
  "error": "解析/校验错误信息",
  "result_tags": { "model_name": "abc" },
  "retry_of": "ObjectId 或 null"
}
```

### 6.3 索引建议

- `business_collections`：`name`（**唯一索引**，Q11 集合名全局唯一）、`members.user_id`（按用户列集合）；
- `data_items`：`{collection_id: 1, created_at: -1}`（集合内分页）、`{collection_id: 1, path: 1}`（路径查询/去重）；
- 标签查询索引：标签名是动态的，建议对 `tags` 子文档使用**通配符索引**（`{"tags.$**": 1}`），或按高频标签显式建索引；需结合实际查询模式评估。

---

## 7. API 设计草案

### 7.1 认证与用户（公共权限）

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/api/v1/auth/login` | 公开 | 登录，返回 JWT |
| POST | `/api/v1/auth/logout` | 登录 | 登出 |
| GET | `/api/v1/auth/me` | 登录 | 当前用户信息 |
| POST | `/api/v1/users` | admin | 创建用户 |
| GET | `/api/v1/users` | admin | 用户列表（分页） |
| PATCH | `/api/v1/users/:id` | admin | 修改用户/角色/禁用 |
| DELETE | `/api/v1/users/:id` | admin | 删除用户 |

### 7.2 集合（公共权限 + 逐集合判定）

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/api/v1/collections` | admin | 创建集合（指定初始集合管理员） |
| GET | `/api/v1/collections` | admin 全部 / 成员本人参与 | 列表（按权限过滤） |
| GET | `/api/v1/collections/:id` | 该集合成员 | 集合详情 |
| PATCH | `/api/v1/collections/:id` | 该集合的集合管理员 | 改基础信息 |
| DELETE | `/api/v1/collections/:id` | admin | 删除集合（级联删元数据） |
| GET | `/api/v1/collections/:id/tags` | 该集合成员 | 查看标签定义 |
| PUT | `/api/v1/collections/:id/tags` | 该集合的集合管理员 | 全量替换标签定义 |
| PATCH | `/api/v1/collections/:id/tags` | 该集合的集合管理员 | 增量修改标签定义 |
| PUT | `/api/v1/collections/:id/script` | 该集合的集合管理员 | 设置/替换刮削脚本路径 |
| GET | `/api/v1/collections/:id/members` | 该集合成员 | 成员列表 |
| POST | `/api/v1/collections/:id/members` | 该集合的集合管理员 | 授权操作工 |
| DELETE | `/api/v1/collections/:id/members/:userId` | 该集合的集合管理员 | 移除成员 |
| PUT | `/api/v1/collections/:id/admin` | admin | 更换集合管理员 |

### 7.3 数据项与刮削（逐集合判定）

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/api/v1/collections/:id/items` | 该集合操作工 | 添加数据项；`auto_scrape=true` 为刮削添加（默认），`false` 为直接添加（可带初始 tags）；均校验路径存在性 |
| GET | `/api/v1/collections/:id/items` | 该集合操作工 | 按标签查询（分页） |
| GET | `/api/v1/items/:itemId` | 所属集合操作工 | 数据项详情 |
| PATCH | `/api/v1/items/:itemId` | 所属集合操作工 | 修改标签值 / 路径（校验新路径存在性） |
| DELETE | `/api/v1/items/:itemId` | 所属集合操作工 | 删除数据项（仅元数据） |
| POST | `/api/v1/items/:itemId/scrape` | 所属集合操作工 | 手动触发刮削 |
| GET | `/api/v1/items/:itemId/scrape-tasks` | 所属集合操作工 | 刮削历史 |
| GET | `/api/v1/scrape-tasks/:taskId` | 相关集合操作工 | 任务详情/状态 |

---

## 8. 刮削系统详细设计要点

### 8.1 脚本执行协议（Q3 已确认，从简）

- 调用方式：`<script_path> <data_path>`，**入参只有一个**（文件或文件夹路径）；
- **输出：stdout 输出 JSON 即可，其余不做要求**（键值对标签对象，如 `{"model_name":"abc","version":"1.2"}`）；
- **成功判据**：产出可解析的 JSON 且通过集合标签定义校验（必填、类型、enum/array/object 递归校验）；
- **失败判据**：无合法 JSON 输出 / 执行超时 / 标签校验失败；退出码仅记录用于诊断，**不作为成功判据**（Q3 未作要求）；
- 限制：执行超时（因 python 脚本可能长时间运行，超时默认放宽，如 30 分钟，且**可配置**）、输出大小上限（如 1 MB）。

### 8.2 并发与队列

- 任务先落库（`pending`），由 Worker 池消费；**多个数据项天然并发刮削**；
- Worker 池规模可配置（如 8~64），防止同时启动大量进程压垮 NFS 或节点；
- 队列实现：MongoDB 轮询（简单可靠，单集合 10 万量级足够）；若未来任务量暴涨再升级消息队列；
- **长任务容忍**：任务无固定 TPS 假设（python 可能长时间运行），超时按 §8.1 可配置；pending 任务在服务重启后可恢复入队；
- 幂等：同一数据项重复刮削，以「后写入者为准」，任务记录保留完整历史。

### 8.3 失败与重试

- 失败原因分类：无合法 JSON / 超时 / 标签校验失败（缺必填、类型不符、enum/array/object 校验失败）；
- 失败不阻塞其他数据项的刮削；
- **手动重试**：基于原任务快照新建任务（不支持定时重刮）。

### 8.4 安全（Q12 已确认：普通子进程即可）

- **路径校验**：数据路径必须位于配置允许的根目录内（防路径逃逸读取任意文件）；
- 脚本路径由集合管理员维护，执行前再次校验可执行性；
- 执行方式：**普通子进程 + 超时 + 输出大小限制**（无需容器/沙箱）；
- 若未来安全要求提升，可平滑演进为沙箱执行。

---

## 9. 非功能需求

| 类别 | 要求 |
|---|---|
| 性能 | 分页查询走索引；刮削任务削峰（Worker 池限流）；避免对 NFS 全树扫描，以「注册制 + 按需刮削」为主 |
| 并发 | 支持多数据项并发刮削；API 侧并发安全（MongoDB 原子更新） |
| 安全 | JWT 认证、bcrypt 密码、RBAC 中间件统一鉴权（公共权限全局 + 集合权限逐集合）、路径逃逸防护、执行超时/输出限制、敏感操作审计 |
| 可靠性 | 刮削失败可手动重试、任务记录完整、服务重启后 pending 任务可恢复（重新入队） |
| 可观测 | 结构化日志（请求日志、任务日志）、审计日志（谁在何时对哪个集合/数据项做了什么） |
| 可维护 | 配置化（NFS 数据根目录、超时、Worker 并发数等）；API 版本化（`/api/v1`） |

---

## 10. 决策记录（Q1~Q12 答复汇总）

| # | 问题 | 最终决策 | 落点 |
|---|---|---|---|
| Q1 | 数据项与文件关系 | 增分两种动作：直接添加 / 刮削添加，均校验路径存在性；删仅删数据库记录；**路径可修改** | §5.4 |
| Q2 | 刮削触发 | 新增数据填入路径即自动刮削；**不支持定时**，仅手动重刮 | §5.5 |
| Q3 | 脚本调用约定 | 入参仅一个路径；输出只要 JSON，其余不做要求 | §8.1 |
| Q4 | 文件/文件夹 | 都支持；后端只判断存在性，可否刮削由脚本决定 | §5.4 |
| Q5 | 标签类型 | string/int/float/bool/date + **enum/array/嵌套 object** | §5.3 |
| Q6 | 查询操作符 | 等值/范围/exists/in；**string 不支持模糊/正则** | §5.6 |
| Q7 | 数据量级 | 单集合约 10 万项；刮削 TPS 不可预估（python 长任务） | §6.1、§8.2 |
| Q8 | 标签定义变更策略 | 采用初稿 | §5.3 |
| Q9 | 集合删除 | 仅 admin，级联删除元数据，不动 NFS 文件 | §5.2 |
| Q10 | 用户来源 | 仅 admin 创建，不支持注册 | §5.1 |
| Q11 | 集合名/标签名 | 集合名全局唯一；标签名不得与保留字段冲突；**标签数量无上限** | §5.2、§5.3、§6.3 |
| Q12 | 脚本执行方式 | 普通子进程即可 | §8.4 |

---

## 11. 后续拆分建议（开发迭代顺序）

> 状态：P0~P5 已全部完成（2026-08 首轮端到端开发），详见 §12。

1. **P0 地基** ✅：项目骨架、配置、MongoDB 连接、日志、统一响应/错误码、JWT 中间件；
2. **P1 RBAC 与用户（R1）** ✅：用户模型、登录、admin 用户管理、鉴权中间件（公共权限全局 + 集合权限逐集合）；
3. **P2 集合与标签定义（R2 前半）** ✅：集合 CRUD、标签定义管理（含 enum/array/object 校验）、成员授权；
4. **P3 数据项 CRUD + 查询（R2 后半）** ✅：直接添加/刮削添加（含路径存在性校验）、增删改查、标签校验、分页查询（等值/范围/in/exists）；
5. **P4 刮削系统（R3）** ✅：脚本注册、任务模型、Worker 池并发、JSON 结果解析入库、失败手动重试、僵死任务回收；
6. **P5 收尾** ✅：审计日志、任务恢复、索引、冒烟级并发验证、文档补齐。

---

## 12. 实现状态与注意事项

### 12.1 实现状态（P0~P5 完成，已通过端到端冒烟测试）

- **代码结构**：`cmd/server`（API 服务）、`cmd/scraper`（刮削子系统，独立进程）、
  `internal/{config, logger, database, model, store, service, scrape, handler, middleware, errno, response, router}`；
- **静态验证**：`go build ./...`、`go vet ./...`、`gofmt` 全部通过；
- **运行验证**：MongoDB + server + scraper 实测，`scripts/e2e.ps1` 冒烟测试 **37/37 断言通过**（支持同库重复执行），覆盖：
  - RBAC：admin 登录、创建用户、非成员访问集合 403、操作工修改标签定义被拒（403）；
  - 集合：创建（含 enum / 嵌套 object 标签定义）、注册刮削脚本、授权/移除操作工；
  - 数据项：直接添加（手动标签）、刮削添加、标签校验入库（int/嵌套 object）、改路径、删除仅元数据（删后 404）；
  - 查询：等值 / 范围（age.gte）/ in / exists 四种操作符；
  - 刮削：自动刮削、手动重刮、**手动标签始终优先**（冲突键保留手动值、刮削补充未产出标签、mixed 状态二次重刮后手动标签仍存活）、刮削历史；
  - **并发**：20 个数据项同时入队刮削，8 个 Worker 全部成功（日志可见同秒并发完成）。

### 12.2 实现中确认的设计细节（对 §5/§8 的细化，非需求变更）

| 细节 | 决策 |
|---|---|
| 手动改标签语义 | `PATCH /items/:id` 的 `tags` 为**全量覆盖**（须满足必填与类型校验），同时写入 `manual_tags` 并将 `tag_source` 置回 `manual`；未传 `tags` 时仅改路径 |
| 标签优先级（刮削 vs 手动） | **手动标签始终优先**：手动标签持久化于 `manual_tags` 字段，刮削结果仅补充手动未产出的标签，冲突键一律保留手动值；存在手动标签时 `tag_source` 置为 `mixed`，仅刮削时为 `scrape`；无论重刮多少次（manual/mixed 状态），手动标签均不被刮削覆盖 |
| 未知标签处理 | 手动添加/修改：**拒绝**并提示未知标签；刮削输出：**忽略**未在标签定义中的键（Q3「输出任意 JSON」的容错） |
| 数据项删除 | 级联删除其刮削任务记录（仅元数据，不动 NFS 文件） |
| 僵死任务回收 | `running` 超过 `scrape.reclaim_seconds`（默认 3600s）的任务由 Worker 重新领取（进程崩溃自愈） |
| 审计日志 | user/collection/item 的敏感变更写入 `audit_logs` 集合（尽力而为，失败不影响主流程） |

### 12.3 注意事项（待办与风险）

1. **冒烟测试环境残留**：验证用 server（:18080）与 scraper 进程、MongoDB 容器（datacenter_testN 测试库）仍在本地运行，可继续体验；正式环境请按 README 重新部署；
2. **生产配置**：`jwt.secret`、bootstrap 管理员密码、`data.root_dir`（NFS 数据根）、scrape 超时等必须通过环境变量覆盖，禁止使用默认值；
3. **真实脚本兼容性**：刮削协议目前仅验证了「可执行文件输出 JSON」；NFS 上的真实 shell/python 脚本需在目标集群实测（超时、输出大小限制、退出码诊断、可执行位）；
4. **压测与单元测试待补充**：当前为冒烟级验证（20 并发）；正式压测（API QPS、刮削任务吞吐）与单元测试（标签校验、权限矩阵、查询构建）未做；
5. **Windows 开发机与 Linux 集群差异**：开发与冒烟测试在 Windows 完成；生产为 Linux + NFS 语义（脚本可执行位、路径分隔符、os.Stat 权限），部署前需在集群回归；
6. **标签数量无上限（Q11）**：标签定义较多时，`tags.$**` 通配符索引的查询性能需在真实数据量（单集合约 10 万项）下评估，必要时按高频标签显式建索引。

---

## 13. 变更记录

| 版本 | 变更 |
|---|---|
| v0.1 | 初稿：需求分解 + 待确认问题 |
| v0.2 | ① 修正 RBAC 权限模型：权限点区分为「公共权限（全局判定）」与「集合权限（逐集合判定）」，集合管理员仅对自己管理的集合有效；② 纳入 Q1~Q12 全部答复（数据项两种添加方式、路径可改、自动/手动刮削、JSON-only 输出协议、enum/array/object 标签类型、无标签数量上限等） |
| v0.3 | 端到端开发完成：P0~P5 全部实现并通过冒烟测试（29/29 断言，含 20 项并发刮削）；新增 §12 实现状态、设计细化决策与注意事项 |
| v0.4 | 标签优先级调整：**手动标签始终优先**——引入 `manual_tags` 字段持久化手动标签，刮削仅补充手动未产出的标签；`PATCH` 全量覆盖并重置 `tag_source=manual`；E2E 增加手动优先断言 |
| v0.5 | ① 前端界面：简洁 DeepSeek 风格 Web UI（登录/集合/数据项/用户管理，hash 路由单页应用，`go:embed` 内嵌二进制单文件部署）；② 单元测试：service/model 包全覆盖（标签定义与值校验、查询过滤器构建、路径/脚本校验、标签合并、角色矩阵、成员角色），`go test ./...` 全部通过；E2E 断言保持 37/37 |
| v0.6 | 前后端按钮与接口全面对接并修复 6 处真实缺陷：① `collectTagValues` 兼容非 form 容器（弹窗添加/编辑数据项）；② `views` 对象补充 `bindLogin`（登录页此前会崩溃）；③ 弹窗预填值改为 body 内查找（配置脚本/编辑描述/编辑数据项三个弹窗此前无法打开）；④ 后端新建集合规范化空 `tag_schema` 为 `[]`；⑤ 前端对 `tag_schema/members` 为 null 的防御；⑥ 退出登录同时清空 `App.user`。新增 jsdom UI 对接冒烟测试（`.tools/ui-test/ui.mjs`，23/23 通过），E2E 回归 37/37 |
| v0.7 | 前端界面重构：① 左侧毛玻璃导航栏（仪表盘/集合管理/刮削管理/权限管理/个人设置）；② 新增仪表盘（概览统计卡片 + 最近集合/任务列表）、刮削管理（全局任务列表 + 状态过滤）、个人设置（信息展示 + 修改本人密码）；③ 毛玻璃质感（backdrop-filter）与卡片悬浮高亮；④ 数据以列表/表格展示，集合以卡片展开；⑤ 后端新增 `GET /stats/overview`、`GET /scrape-tasks`（全局列表，按成员范围过滤）、`POST /auth/password`（原密码错误返回 400 避免误登出）；jsdom UI 测试扩至 31/31，E2E 37/37 |
| v0.8 | DQL 查询功能 + 集合管理重构：① 新增 DQL 引擎（`internal/dql`：词法/语法/AST/MongoDB 过滤构建），支持 AND/OR（AND 优先）、括号、`= != > >= < <= IN EXISTS LIKE`、`collection` 关键字限定跨集合范围，按集合标签定义做类型校验与值规范化（含跨集合类型不一致的宽松数值转换），`POST /dql/query` 接口；② 前端新增「数据查询」导航（DQL 输入 + 示例/清空 + 结果列表（所属集合/路径/标签/状态）+ 新增数据项（选集合动态标签表单）/详情/重刮/删除）；③ 集合管理重构：详情页仅保留数据管理（概览/标签定义/脚本/成员），不再展示数据，创建/删除集合仅 admin（原有），集合管理员仅可授予/回收操作工角色；④ 新增 DQL 单元测试（解析优先级/字面量/错误语句/过滤器构建/collection 提取）与 UI 断言扩至 33/33，E2E 37/37 |
| v0.8.1 | 前端查询提示：①「数据查询」页新增「💡 查询示例」提示卡片（按场景分类，点击自动填入并查询）；② 新增独立「📖 DQL 语法」帮助页（侧边导航，语法规则表 + 示例复制）；③ 修复 jsdom 测试环境下 `App.init()` 被 DOMContentLoaded 重复触发导致路由双渲染的偶发失败（init 幂等守卫 + 测试等待登录页渲染），UI 断言扩至 37/37 并连续多轮稳定通过 |
| v0.9 | 数据关联关系功能上线（按《关联关系设计方案.md》v1.2）：① 新增 `data_relations` 边集合与完整索引（含单父部分唯一索引）；② 关联 API：建边/批量建边（Q7）/出边入边分页（含对端摘要）/改删边/层级树（BFS+深度上限）/关联徽标；③ **单父约束与父链环检测**（Q1）；④ **集合删除策略 delete_policy**（children: deny/cascade/detach；incoming: deny/detach，默认 deny）+ 删除预检 delete-impact + 策略化连锁删除（cascade/force/dry_run，先删边后删 item，审计）；⑤ P3 优化一并上线：**物化路径 ancestors**（子树单查询）、**DQL parent/ancestor 关系字段**、仪表盘关系统计、`GET /items/search` 轻量搜索；⑥ 前端：数据项详情关联卡片（出/入边、添加弹窗（搜索+多选批量+layer_order/role 快捷字段）、树视图）、策略化删除确认弹窗、集合详情删除策略配置卡、DQL 结果行关联徽标、帮助页与示例更新；UI 断言扩至 49/49，E2E 37/37，单测全绿 |
| v0.10 | ① 本地一键演示环境：`scripts/mongo-up.ps1`（Docker 重建 MongoDB 并挂载**命名数据卷** `datacenter-mongo-data`，容器重建不丢数据）；② 新增演示数据灌入工具 `cmd/seeder`（`scripts/seed.ps1` 一键执行）：按计算光刻领域知识生成 **model/case/layout/layer** 四集合（标签覆盖 string/int/float/bool/date/enum/array/object）、真实文件树（.nfsdata/litho/...）与关联关系（layout→layer 父子 17762 条 + case→model/layout 引用 6000 条，物化路径 ancestors 写入）；默认灌入约 2.6 万数据项，可重复执行（-fresh 清空重建）；③ 新增 `scripts/start-server.ps1` / `start-scraper.ps1` / `verify.ps1`（登录→概览→DQL 标签/关系查询验证）；④ 修复真实缺陷：**DQL 纯 collection/parent/ancestor 条件（无标签条件）时 `BuildFilter` 返回 nil 导致 `filter["collection_id"]` 赋值 panic（500）**——Query 中对 nil filter 初始化为空 `bson.M{}`；⑤ store 新增 `ItemStore.InsertMany` / `RelationStore.InsertMany`（种子数据批量导入）；⑥ 修复 UI 测试脚本缺陷：`waitFor` 对返回值做 truthy 判断，`parseInt("0")=0` 为 falsy 导致**任务统计在任务数为 0 时误报失败**（返回 `true` 而非数值）；回归：单测全绿，UI 冒烟 **50/50**，DQL 关系查询（ancestor 子树 / parent 父项）经 verify.ps1 实测通过 |
| v0.11 | ① **admin 全局只读浏览权**（§4.3 权限矩阵同步修订）：集合详情/标签定义（`GET /collections/:id`、`/tags`、`/members`）、数据项详情（`GET /items/:id`）、关联视图（出/入边、树、删除预检 delete-impact）对 admin 放行——与「查看全部集合列表」「DQL admin 全量查询」一致，修复 admin 点进集合详情被拒（"不是该集合的成员"）的体验断裂；**写操作一律不变**（修改信息/标签定义/脚本/成员、数据项增删改/刮削、删除仍按集合角色，admin 非成员实测 403）；② 前端「编辑描述」按钮改为仅集合管理员显示（此前 admin 可见但点击 403）；③ DQL 查询页错误提示从页面顶部移至「查询结果」卡片内（`#q-msg` 移入结果卡），集合不存在/语法错误不再在页面上方弹红色横幅；回归：单测全绿、UI 冒烟 **50/50**、E2E **37/37** |
| v0.12 | **admin 最高权限（全权，取代 v0.11 的只读方案）**：`PermissionChecker` 注入 UserStore，admin 对任意集合的**查看/修改全部放行**——集合信息/标签定义/刮削脚本/成员/删除策略、数据项增删改/手动刮削、关联边增删改/策略化删除等一切集合级操作均无需加入 members（等同各集合的集合管理员+操作工）；非 admin 权限模型不变（逐集合 members 判定，含 admin 专属公共权限：集合创建/删除/更换管理员）。前端集合详情页将 admin 视为集合管理员（显示全部管理按钮与徽标）。实测：admin 非成员改标签定义/授权成员/改描述/配置脚本/增删数据项/建关联边均放行，非成员普通用户仍 403、操作工改标签定义仍 403。§4.3 权限矩阵同步更新；回归：单测全绿、UI 冒烟 **50/50**、E2E **37/37** |
| v0.13 | **UI 提示整体重构（全局 Toast）**：新增右下角毛玻璃 Toast 提示系统（`#toast-container`，成功 3.2s/错误 5.2s 自动消失、点击关闭、悬浮于弹窗之上），`setMsg` 统一重定向为 Toast——**全部页面与弹窗的成功/失败/信息提示不再内嵌在页面顶部或卡片内的红色横幅**（DQL 报错、删除策略保存、授权成员、表单校验、刮削触发、删除结果等 90+ 处调用一次生效）；路由级渲染错误同样改为 Toast；ui.mjs 9 处消息断言同步改为 Toast 查询；**确认 admin 修改删除策略/授权操作工在 v0.12 已放行**（实测 code=0，此前报"您不是该集合成员"为旧进程/旧缓存所致）；回归：单测全绿、UI 冒烟 **50/50**、E2E **37/37** |
| v0.14 | **整体权限排查 + 前端权限判断点统一**：① 修复数据项详情页 `canOperate = !!myMember` 未含 admin 的遗漏——admin 查看**非成员集合**的数据项详情时「编辑/手动刮削/添加关联」按钮不显示（后端早已放行），现统一为 `!!myMember \|\| App.isAdmin()`；② 全面盘点前端 27 处权限判断点，确认三类（admin 专属 `App.isAdmin()`、集合管理 `isColAdmin`（v0.12 含 admin）、数据操作 `canOperate`（本次含 admin））已全部与后端 admin 全权对齐；③ 后端逐接口核对：集合（Get/List/写操作）、数据项（增删改查/搜索）、刮削（手动触发/历史/全局列表）、关联（建/改/删/树/预检/徽标）、DQL、统计——admin 全部放行，非 admin 严格按逐集合 members 判定；④ UI 测试新增 3 项回归断言（admin 非成员集合详情页展示「手动刮削/编辑/添加关联」）并修复 2 处测试脆弱点（`#qa-col` 程序化赋值补派发 change 事件）；⑤ **版本自证机制**：新增 `internal/version` 包（`make build VERSION=x` 可注入），`GET /healthz` 返回 `version` 字段，前端侧边栏底部常显版本号——便于确认部署实例的代码版本（排查"行为与预期不符"类问题第一步先看版本）；回归：单测全绿、UI 冒烟 **53/53**、E2E **37/37** |
| v0.15 | **鉴权中间件存活性加固（幽灵身份根治）**：`Auth` 中间件此前只校验 JWT 签名/过期，**不查 users 表**——用户被删除/重建（如 v0.10 MongoDB 重建清库后 admin 的 `_id` 变化）后，旧 token 签名依然有效，`CurrentUserID` 指向不存在的用户：界面显示管理员（token 载荷 role=admin），但权限判定查库找不到用户 → 被当作普通用户 → 出现"admin 重刮报'您不是该集合成员'(3003)"的幽灵身份问题。现加固：JWT 校验后**查库校验用户存在且 active**（不存在/禁用 → 401 强制重新登录），并采用**实时角色覆盖 token 载荷**（降权/升权即时生效）；模拟幽灵 token（uid 不存在）实测 401、正常登录 200；另修复前端版本号显示双 v（`v`+`v0.14`→`vv0.14`）的瑕疵；回归：UI 冒烟 **53/53**、E2E **37/37** |
| v0.16 | **安全加固上线（按《安全增强方案.md》v1.1）**：P0-1 **登录防爆破**（同一 IP+用户名连续 5 次失败锁定 15 分钟，文案与密码错误一致防枚举，登录失败/成功写审计）；P0-2 **默认密钥/口令启动校验**（`mode: release` 下默认 `jwt.secret` 或默认管理员口令 → 拒绝启动并提示覆盖，debug 仅告警）；P0-3 **密码策略统一**（≥8 位 + 字母 + 数字，创建用户/重置/个人改密三处共用校验）；P0-4 **请求体限制**（全局 4MB，超出 413，新增 `ErrBodyTooLarge` 与 `bindJSON` 统一绑定入口）；P1-5 **安全响应头**（nosniff / X-Frame-Options: DENY / Referrer-Policy + `SetTrustedProxies(nil)` 防伪造 ClientIP，CSP 后置）；P1-6 **安全审计事件**（`auth.login_success/login_fail`、`auth.permission_denied` 写入 audit_logs）；P1-7 **改密吊销旧 token**（`users.password_version` + JWT `pwd_ver`，改密/重置密码后旧 token 立即 401，存量 token 平滑兼容）；P1-8 **JWT 过期 72h→12h** + 部署指南补充 secret 轮换流程；新增单测（密码策略/防爆破），E2E 密码同步更新为合规强度；回归：单测全绿、安全验证 **13/13**、UI 冒烟 **53/53**、E2E **37/37**、审计事件实测落库 |
| v0.17 | **系统优化 B1 批上线（按《系统优化方案.md》v1.1）**：① **标签查询索引**——`data_items` 新增 `tags.$**` 通配符索引，DQL 标签过滤由集合内扫描升级为索引直取（explain 实测 IXSCAN、totalDocsExamined=1）；② **数据项批量导入**——`POST /collections/:id/items/batch`（≤500 条，逐条校验路径/标签，返回成功失败明细，全局 auto_scrape 默认 false），前端「批量添加」弹窗（路径清单 textarea + 失败明细内联展示）；③ **DQL 增强**——`ORDER BY field ASC|DESC` 排序（int/float/date 标签，含类型校验）、`POST /dql/aggregate` 分组统计（string/enum 字段，分布直方图前端展示）、结果导出 CSV（当前页，UTF-8 BOM 兼容 Excel）；④ **审计日志治理**——TTL 保留策略（默认 180 天，`DATACENTER_AUDIT_RETENTION_DAYS` 可配）+ `GET /audit-logs` 查询接口（admin 专属，action 前缀/用户名过滤，含操作者名）；⑤ **备份脚本** `scripts/backup.ps1`（mongodump + NFS 目录打包 + 保留 N 份轮转，docker exec 降级路径）；⑥ 附带修复：openAdd 集合切换渲染竞态（renderSeq 防护）、前端 API 20s 超时防挂起、ui.mjs 测试稳定性（stableFetch/suffix 唯一化/waitFor 20s）；回归：单测全绿、批量导入验证 11/11、DQL 增强验证 11/11、E2E **37/37**；UI 冒烟存在 jsdom 环境间歇抖动（服务端请求响应全部正常，见 系统优化方案 §8） |
