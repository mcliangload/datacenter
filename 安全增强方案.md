# 安全增强方案

> 版本：v1.1（**P0 四项 + P1 核心四项已实施并验证通过**，2026-08；CSP 与本地 MongoDB 认证按 §4.5/§4.9 后置）
> 关联系统：数据中心（Go + Gin + MongoDB + NFS）
> 范围：认证/授权/传输/输入防护/审计/运维 六个维度的安全增强，分级（P0 必修 / P1 建议 / P2 可选）

## 0. 结论摘要

**当前基础防线已扎实**（bcrypt 密码、JWT + 用户存活性校验、双层 RBAC、注入防护、路径防逃逸、
刮削沙箱约束、审计日志、无 cookie 防 CSRF）；**缺口集中在对抗性防御（防爆破）、默认值防护（防裸奔）、
纵深防御（安全头/安全审计事件/旧 token 吊销）**。

建议实施范围：**P0 四项 + P1 核心四项（安全基础头 / 安全审计事件 / 改密吊销旧 token / JWT 收紧）**，
CSP 与本地 MongoDB 认证后置（见 §4.5、§4.6 说明）。

## 0.1 实施状态（v1.1 回填）

| 项 | 状态 | 验证 |
|---|---|---|
| P0-1 登录防爆破（5 次失败锁 15 分钟） | ✅ 已实施 | 安全验证 13/13；审计 login_fail 落库 |
| P0-2 默认密钥/口令启动校验（release 拒绝启动） | ✅ 已实施 | release+默认 secret/密码 均实测拒绝启动 |
| P0-3 密码策略（≥8 位 + 字母 + 数字，三处统一） | ✅ 已实施 | 单测 + API 弱密码被拒实测 |
| P0-4 请求体限制（4MB → 413） | ✅ 已实施 | 5MB body 实测 413 |
| P1-5 安全基础头（nosniff/X-Frame/Referrer + TrustedProxies） | ✅ 已实施 | 响应头实测；CSP 后置 |
| P1-6 安全审计事件（login_success/fail、permission_denied） | ✅ 已实施 | audit_logs 实测落库 |
| P1-7 改密吊销旧 token（pwd_ver） | ✅ 已实施 | 改密后旧 token 401 实测 |
| P1-8 JWT 过期 72h → 12h + 部署指南轮换流程 | ✅ 已实施 | 配置生效 |
| 回归 | ✅ | 单测全绿、UI 冒烟 53/53、E2E 37/37 |

## 1. 安全现状评估（v0.15 代码核查结论）

| 维度 | 现状 | 评价 |
|---|---|---|
| 密码存储 | bcrypt(DefaultCost=10)；登录不区分"用户不存在/密码错误"（防账号枚举）；disabled 用户拒绝登录 | ✅ |
| 认证 | JWT HS256 + issuer + 过期校验；**v0.15 用户存活性校验（存在+active，否则 401）+ 实时角色覆盖 token 载荷** | ✅ |
| 授权 | 双层 RBAC：全局角色（admin/user）+ 逐集合 members（collection_admin/operator）；admin 全权（v0.12）；admin 自我保护（不能改/删自己、不能禁用/删除最后一个管理员） | ✅ |
| 输入防护 | DQL 结构化 bson 构建（无 NoSQL 注入面）；LIKE 正则转义（regexp.QuoteMeta）；标签类型/enum/array/object 校验；路径防逃逸（绝对路径 + root_dir 前缀校验） | ✅ |
| 刮削沙箱 | 脚本输出 1MB 上限、超时 1800s、JSON 解析 + 标签校验、退出码仅诊断 | ✅ |
| 会话 | 无 cookie（天然防 CSRF）；HTTP ReadHeader/Read/Write 超时已配置 | ✅ |
| 审计 | 业务写操作（集合/数据项/关系/刮削/用户）写入 `audit_logs` | ⚠️ 无安全事件 |
| 错误处理 | 统一响应体不泄露内部细节（WithCause 不回传客户端）；日志中 Authorization 脱敏 | ✅ |
| 部署 | 部署指南 §6 已有安全清单（HTTPS/防火墙/Mongo 鉴权/备份/非 root） | ⚠️ 代码无强制 |

## 2. 风险清单与分级

| 级别 | 风险 | 现状 | 方案 |
|---|---|---|---|
| 🔴 P0 | 登录暴力破解 | 登录接口无速率限制 | §4.1 失败锁定 |
| 🔴 P0 | 默认密钥/口令裸奔 | 仅文档提醒覆盖，代码不拦 | §4.2 启动强校验 |
| 🔴 P0 | 弱密码策略 | 仅最小 6 位 | §4.3 8 位+字母数字 |
| 🔴 P0 | 超大请求体 DoS | Gin 无 body 上限 | §4.4 4MB 限制 |
| 🟠 P1 | 缺少安全响应头 | 无 nosniff/X-Frame 等 | §4.5 基础头（CSP 实验） |
| 🟠 P1 | 安全事件无审计 | 只有业务写操作审计 | §4.6 登录/拒绝/锁定事件 |
| 🟠 P1 | 改密后旧 token 仍有效 | 72h 内继续可用 | §4.7 pwd_ver 吊销 |
| 🟠 P1 | JWT 过期过长 | 固定 72h | §4.8 收紧为 12h |
| 🟠 P1 | MongoDB 无认证（本地） | Docker 未开 --auth | §4.9 建议后置 |
| 🟡 P2 | CORS 白名单 / 敏感操作二次认证 / 审计查询界面 / 验证码 / 2FA | — | §5 |

## 3. 总体设计原则

1. **默认安全**：生产（release）拒绝默认配置启动；dev 模式仅告警，不影响本地开发。
2. **纵深防御**：认证（JWT+存活性）→ 授权（RBAC）→ 输入防护 → 限流 → 审计，多层独立。
3. **不破坏现有体验**：锁定提示与密码错误同文案（防枚举）；存量弱密码用户不受影响（改密时生效）；
   旧 token 兼容（pwd_ver 缺省视为 0）。
4. **最小依赖**：全部中间件/校验用标准库与现有依赖实现，不引入 Redis/验证码等外部组件。

## 4. 详细设计

### 4.1 P0-1 登录防爆破（`internal/service/auth.go` + `internal/middleware`）

- **机制**：内存级失败计数器 `map[loginKey]*failWindow`，`loginKey = ip + "|" + username`；
  连续 **5 次失败 → 锁定 15 分钟**（阈值/时长做成常量，后续可配置化）。
- **行为**：
  - 锁定期内登录直接返回与"用户名或密码错误"**相同文案**（不泄露是否锁定，防账号枚举）；
  - 登录成功清空该 key 的失败计数；
  - 失败/锁定事件写审计（`auth.login_fail` / `auth.login_locked`）。
- **清理**：成功清零 + 定时（或惰性）清理过期窗口，防止内存膨胀。
- **获取 IP**：`c.ClientIP()`；反向代理后需 TrustedProxies 配置（部署指南 Nginx 场景需设
  `gin.SetTrustedProxies` 或关闭信任，见 §4.5 权衡）。
- **影响面**：单实例部署足够；多实例需分布式计数（Redis，标注后续）；进程重启后计数清零（可接受）。
- **验证**：连续 5 次错误密码 → 第 6 次正确密码也拒绝（锁定文案）；锁定 15 分钟（测试可用
  短阈值/短锁定参数化）后恢复；锁定期间审计有 `login_locked` 记录。

### 4.2 P0-2 默认密钥/口令启动校验（`cmd/server/main.go` / `internal/config`）

- **机制**：配置加载后、服务启动前校验：
  - `server.mode == release` 且 `jwt.secret == "datacenter-dev-secret"` → **拒绝启动**，
    `Fatal("生产环境禁止使用默认 JWT secret，请通过 DATACENTER_JWT_SECRET 覆盖")`；
  - `server.mode == release` 且 bootstrap 密码为默认 `admin123` → **拒绝启动**（同样提示覆盖）；
  - `debug` 模式：仅 `Warn` 告警，不拦截（本地开发不受影响）。
- **注意**：bootstrap 密码默认值判断需同时考虑 env 覆盖（`DATACENTER_BOOTSTRAP_ADMIN_PASSWORD`）。
- **验证**：`DATACENTER_SERVER_MODE=release` 启动 → 拒绝；`debug` 启动 → 正常 + 告警日志。

### 4.3 P0-3 密码策略统一（`internal/service/password.go` 新文件）

- **规则**：`len >= 8` 且包含至少 1 个字母（unicode letter）且至少 1 个数字。
- **落点**（三处共用同一校验函数）：
  - 创建用户（`UserService.Create`）；
  - admin 重置密码（`UserService.Update` 的 password 分支）；
  - 个人修改密码（`AuthService.ChangePassword`，原 6 位校验替换）。
- **兼容**：存量弱密码用户不强制（下次改密时生效）。
- **验证**：三处接口分别用弱密码（短/纯数字/纯字母）→ 400；合法密码 → 成功。

### 4.4 P0-4 请求体大小限制（`internal/middleware/bodylimit.go` 新文件）

- **机制**：Gin 全局中间件，`http.MaxBytesReader` 限 **4MB**，超出返回 413（`errno` 新增或复用）。
- **覆盖**：全部 POST/PUT/PATCH；批量建边（≤200 条）、标签表单等场景远小于 4MB，无感。
- **验证**：构造 >4MB body → 413；正常请求不受影响。

### 4.5 P1-5 安全响应头（`internal/middleware/security.go` 新文件）

- **基础三项（本轮实施，零风险）**：
  - `X-Content-Type-Options: nosniff`（防 MIME 嗅探）；
  - `X-Frame-Options: DENY`（防点击劫持）；
  - `Referrer-Policy: same-origin`。
- **CSP（实验项，后置）**：前端无外部 CDN（脚本全 `'self'`），但模板大量内联 `style` 属性 →
  需 `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:;`
  并浏览器实测全部页面后启用。决策点：用户确认后另行实施。
- **顺带**：`gin.SetTrustedProxies(nil)`（或按部署拓扑配置），避免 `ClientIP` 被 X-Forwarded-For 伪造
  （与 §4.1 防爆破的 IP 维度联动）。

### 4.6 P1-6 安全审计事件（`internal/service/auth.go` / `internal/service/permission.go`）

- **新增审计事件**（写入现有 `audit_logs`）：
  - `auth.login_success` / `auth.login_fail`（含锁定标记）——登录成功/失败；
  - `auth.permission_denied` —— 403 权限拒绝（在 `PermissionChecker.RequireRole` 拒绝分支、
    handler 层 403 出口统一记录：操作者、目标集合、所需角色）。
- **权衡**：登录失败审计可能高频——仅记录失败（含 IP 与用户名），不记录密码。
- **验证**：错误密码登录 → 审计出现 `login_fail`；非成员访问 → `permission_denied`。

### 4.7 P1-7 修改密码吊销旧 token（`internal/model/user.go` + `internal/model/claims.go` + `Auth` 中间件）

- **机制**：
  - `users` 表新增 `password_version int`（默认 0，迁移兼容存量数据）；
  - JWT claims 新增 `pwd_ver int`（签发时写入用户当前版本）；
  - 修改密码 / admin 重置密码时 `password_version + 1`；
  - `Auth` 中间件（v0.15 存活性校验处）比对 `claims.pwd_ver == user.password_version`，
    不匹配 → 401（强制重新登录）。
- **兼容**：存量 token 无 `pwd_ver` claim → 解析为 0；存量用户 version 缺省为 0 → 无需强制重登；
  首次改密后旧 token（含无 claim 的）全部失效。
- **价值**：密码泄露场景下，改密同时吊销已签发 token。
- **验证**：登录 → 改密 → 旧 token 请求 → 401；新登录 token → 200。

### 4.8 P1-8 JWT 过期收紧（`config/config.yaml` + 部署指南）

- 默认 `jwt.expire_hours: 72` → **12**（可配置，内网可放宽）；
- 部署指南补充：**secret 轮换流程**（更换 `jwt.secret` 后所有存量 token 失效，用户重新登录；
  建议在维护窗口执行并配合 §4.7 的 pwd_ver 兼容）。
- **验证**：签发后 12h 过期（配置可测）。

### 4.9 P1-9 本地 MongoDB 开启认证 —— **建议后置**

- 影响面：容器启动参数（`--auth` + 初始化账号）、全部连接串（config.yaml + start-server/
  start-scraper/seed/verify 四个脚本 + e2e/ui 测试）、已有数据卷需先建用户再开 auth；
- 本地开发价值低；生产按 部署指南 §6 执行（独立账号 + `authSource=admin` + 27017 不暴露公网）。

## 5. P2 可选清单（内网后置）

| 项 | 说明 |
|---|---|
| CORS 白名单 | 前后端分离部署时显式允许来源 |
| 敏感操作二次认证 | 删除集合 / 级联删除时输入密码确认 |
| 审计日志查询界面 | 现有 `audit_logs` 有存储无查询界面 |
| 登录验证码 | 公网部署时叠加防爆破 |
| 2FA / SSO | 内网组织级身份体系接入 |

## 6. 实施顺序与回归

1. P0-4 请求体限制（独立、零风险）
2. P0-3 密码策略（独立）
3. P0-2 启动校验（独立）
4. P0-1 登录防爆破（含审计事件，先于 P1-6）
5. P1-6 安全审计事件（权限拒绝出口）
6. P1-7 改密吊销（模型 + claims + 中间件）
7. P1-5 基础安全头（+ TrustedProxies）
8. P1-8 JWT 收紧 + 部署指南补充
9. 回归：单测（新增：密码策略/防爆破/安全头/bodylimit）、UI 冒烟、E2E 全量；
   手动验证：幽灵 token 401（沿用 v0.15 验证脚本思路）、锁定-解锁闭环、改密吊销。

## 7. 待确认决策点

- [ ] P0-1 锁定阈值/时长：默认 5 次 / 15 分钟，是否调整？
- [ ] P0-3 密码策略：字母+数字即可，是否需要特殊字符？
- [ ] P1-5 CSP 是否本轮启用（默认后置，基础三项先行）？
- [ ] P1-8 JWT 过期：12h 是否合适（内网可放宽）？
- [ ] P1-9 本地 Mongo 认证是否后置（默认后置）？

## 8. 变更记录

| 版本 | 变更 |
|---|---|
| v1.0 | 初稿：现状评估 + P0/P1/P2 分级方案 + 实施顺序与验证方式（待评审） |
| v1.1 | P0 四项 + P1 核心四项全部实施并验证（见 §0.1）；新增单测（密码策略/防爆破）；部署指南补充安全项与 JWT 轮换流程；需求分解 v0.16 同步 |
