# 数据关联关系设计方案

> 版本：v1.3（评审结论 Q1~Q8 已确认；**P1/P2/P3 已全部实现并上线**，2026-08）
> 关联系统：数据中心（Go + Gin + MongoDB + NFS，集合/数据项/标签/DQL 现有体系）
> 范围：数据项之间的关联关系建模、存储、查询、连锁删除、权限与前端 UX 设计（本方案不含代码实现）

## 0. 实现状态（v1.3 回填）

| 阶段 | 内容 | 状态 |
|---|---|---|
| P1 后端 | data_relations 模型 + 索引（含单父部分唯一索引）；建边/批量建边/出边入边分页（对端摘要）/改删边/层级树（BFS+深度/每层上限）/关联徽标；**单父约束 + 父链环检测**；**delete_policy 集合配置 + 删除预检 delete-impact + 策略化连锁删除（cascade/force/dry_run，先删边后删 item，审计）**；权限与审计；`GET /items/search` 轻量搜索 | ✅ 已完成 |
| P2 前端 | 数据项详情关联卡片（出/入边、添加弹窗（搜索+多选批量+layer_order/role 快捷字段）、树视图）；策略化删除确认弹窗（影响清单+级联/强制勾选+deny 拒绝提示）；集合详情删除策略配置卡；DQL 结果行关联徽标 | ✅ 已完成 |
| P3 优化 | 物化路径 ancestors（建/删父边逐层维护，子树单查询）；DQL 关系字段 `parent`/`ancestor`（帮助页/示例同步）；仪表盘关系统计（按类型） | ✅ 已完成 |

验证：UI 冒烟测试 **50/50**（关联全链路：添加→出边→树→DQL parent→deny 拒绝→级联删除）、E2E **37/37**、`go test` 单测全绿（策略校验/DQL 关系提取）、`build/vet/gofmt` 干净。
实现落点：`internal/{model/relation.go, store/relation.go, service/relation.go, handler/relation.go}`、`internal/web/static/views.js`、`.tools/ui-test/ui.mjs`。

---

## 1. 背景与目标

### 1.1 背景

当前数据项（文件/文件夹）彼此孤立：只能通过标签描述自身，无法表达数据之间的**关系**。实际场景中数据是网状关联的，例如：

- **自动化用例**：用例数据调用了其他模块的数据（model、recipe、版图），需要表达「用例 → 依赖数据」；
- **版图（layout）**：一个版图包含很多层（layer），层还可能继续包含子单元，需要表达「版图 → 层 → 单元」的层级；
- **跨模块引用**：多个用例引用同一份 model 数据，需要表达「谁引用了谁」，便于变更影响分析（改 model 会影响哪些用例）。

### 1.2 目标

1. 在现有「集合 → 数据项 → 标签」体系之上，增加**数据项之间的有向关联**；
2. 支持三类语义：**父子关系（包含/层级）、引用关系、调用关系**；
3. 提供完整的关联 CRUD、双向查询、层级树查询、连锁删除与权限控制；
4. 提供前端 UX 设计稿，与现有 DeepSeek 风格界面（毛玻璃/侧边导航/列表化）一致；
5. 不影响现有数据项、标签、DQL、刮削体系（向后兼容，分阶段落地）。

### 1.3 非目标（本期）

- 不做数据血缘的自动推导（从刮削脚本/执行日志自动发现调用关系）；
- 不做可视化关系图谱（v3 可扩展）；
- 不做 DQL 的 JOIN 语义（v2 扩展，见 §6.5）。

---

## 2. 术语与关联类型语义

| 术语 | 说明 |
|---|---|
| 关系边（Relation） | 一条有向关联：起点 `from_item_id` → 终点 `to_item_id`，带类型与可选属性 |
| 出边（Outgoing） | 以该数据项为起点的关系 |
| 入边（Incoming） | 以该数据项为终点的关系 |
| 目标端摘要 | 查询关系时返回对端数据项的摘要（路径、关键标签、所属集合） |

### 2.1 三种关联类型

| 类型 | 枚举值 | 语义 | 典型场景 | 多重性 | 删除语义 |
|---|---|---|---|---|---|
| 父子关系 | `parent_child` | 包含/层级/组合：父拥有子 | 版图 → 层 → 单元；目录式组织 | 一个父可有多个子；**一个子有且仅有一个父（单父树，Q1 已确认）** | 按集合删除策略处理（默认：有子时拒绝，见 §7） |
| 引用关系 | `reference` | 弱引用：A 引用 B，不拥有 | 用例引用 model；方案引用版图 | N:N | 按集合删除策略处理（默认：被引用数>0 时拒绝，见 §7） |
| 调用关系 | `call` | 运行时依赖：A 调用/执行 B | 自动化用例调用其他模块数据/脚本 | N:N | 同引用；meta 可记录调用参数 |

> 三类关系**方向均有意义**：父子是「属于/包含」，引用/调用是「依赖」。反查（入边）是变更影响分析的关键能力：改一份 model，能立刻知道哪些用例在引用/调用它。

### 2.2 关系属性（meta）（Q4 已确认：需要）

每条边可携带可选 JSON 属性，例如：

- 父子：`{"layer_order": 2}`（层序，用于树视图排序）、`{"role": "metal"}` —— 父子类型提供 **layer_order / role 快捷字段**（前端表单直填，存储仍为 meta JSON）；
- 引用：`{"alias": "model_v1"}`
- 调用：`{"param": "--layer 2", "script": "run_case.sh"}`

meta 不参与索引（通用查询用 type/direction），但可在树视图/列表展示，并支持 `PATCH` 单独更新；
建边时 meta 为可选，缺省 `{}`。

---

## 3. 总体设计

### 3.1 存储模型选型：独立「关系边集合」（推荐）

```
方案 A（推荐）：独立集合 data_relations
  data_items 不加关系字段，关系全部外置为边
  优点：出边/入边天然双向可查；类型可扩展；审计友好；批量删除简单；
        不污染数据项文档（避免大数组文档 16MB 风险）；支持跨集合关系
  缺点：查"某 item 的关系"需一次边查询 + 一次对端摘要查询（应用层组装）

方案 B：数据项内嵌数组（children/refs/calls）
  优点：读层级直接
  缺点：反查需要全表扫描或双写；内嵌数组更新与并发冲突成本高；跨集合引用难表达；
        大层级（数百层）导致文档膨胀
  结论：不采用
```

### 3.2 架构示意

```
┌──────────────┐    ┌──────────────────────────┐
│  前端（UX 稿  │    │  Gin 后端                  │
│  见 §9）      │───▶│  ├─ RelationService       │
└──────────────┘    │  │   - 建边/删边/改边      │
                    │  │   - 出边/入边/树查询     │
                    │  │   - 连锁删除编排         │
                    │  └──────────┬───────────────┘
                    │             │
        ┌───────────▼───────────┐ │  ┌────────────────────┐
        │ MongoDB               │ │  │ NFS（不动）          │
        │ ├─ data_items         │◀┘  │ 文件本体/刮削脚本    │
        │ ├─ data_relations ★新 │    └────────────────────┘
        │ ├─ business_collections
        │ └─ audit_logs（关联操作审计）
        └───────────────────────┘
```

---

## 4. 数据模型（MongoDB）

### 4.1 data_relations（新增集合）

```json
{
  "_id": "ObjectId",
  "collection_id": "ObjectId",     // 起点所属集合（冗余，用于集合级联删除）
  "from_item_id": "ObjectId",      // 起点数据项
  "to_item_id": "ObjectId",        // 终点数据项
  "type": "parent_child | reference | call",
  "meta": { "layer_order": 2, "alias": "model_v1" },   // 可选，任意 JSON
  "created_by": "ObjectId",        // 建边人
  "created_at": "ISODate",
  "updated_at": "ISODate"
}
```

约束：

- `from_item_id != to_item_id`（禁止自环）；
- `type` 必须是枚举之一；
- 同一 `(from, to, type)` **唯一**（唯一索引兜底，重复建边幂等返回已存在）；
- 允许同一对 item 存在不同类型边（既引用又调用）；
- **单父约束（Q1 已确认）**：`parent_child` 类型下，一个数据项最多只能有一条入边（部分唯一索引兜底），即每个子节点有且仅有一个父；建父边时若目标已有父 → 409「该数据项已有父节点」；
- 支持**跨集合**关系（起点/终点可属于不同集合，权限见 §8）。

### 4.2 索引设计（完整清单）

| 集合 | 索引 | 用途 |
|---|---|---|
| `data_relations` | `{from_item_id: 1, type: 1, created_at: -1}` | 出边分页查询 |
| `data_relations` | `{to_item_id: 1, type: 1, created_at: -1}` | 入边分页查询（变更影响分析） |
| `data_relations` | `{from_item_id: 1, to_item_id: 1, type: 1}` **唯一** | 幂等防重 |
| `data_relations` | `{collection_id: 1}` | 集合级联删除边 |
| `data_relations` | `{to_item_id: 1}` **唯一 + partialFilter `{type: "parent_child"}`** | **单父约束兜底（Q1）**：同一子节点只允许一条父子入边 |
| `data_relations` | `{to_item_id: 1}`（可选，若与上合并可省） | 删除 item 时按入边清理 |
| `data_items`（v2 新增） | `{ancestors: 1}` | 物化路径：子树单查询（§6.3） |

> 数据量评估：单集合 10 万数据项、平均每项 5 条边 → 50 万边/集合，以上索引完全可承载；
> 关联数徽标（出/入边计数）走 from/to 索引的 `countDocuments`，无需额外索引。

### 4.3 与现有模型的关系

- 数据项、集合、标签、刮削任务文档**均不修改字段**（向后兼容）；
- 集合级联删除（现有）扩展为：先删数据项，再按 `collection_id` 批量删边；
- 审计沿用 `audit_logs`，新增 action：`relation.create / relation.update / relation.delete / item.delete_cascade`。

---

## 5. 关系约束与校验（服务层）

1. **存在性**：建边前校验 `from_item_id`、`to_item_id` 存在；
2. **自环**：起点 ≠ 终点；
3. **类型**：`type` 枚举校验；
4. **重复**：唯一索引 + 先查后插（返回已存在或 409）；
5. **单父与环检测（Q1 已确认）**：父子关系为单父树，无多父环；仅需在建父边时沿**父链上溯**（入边逐层递归）检查「新父是否为自己或自己的后代」，命中则拒绝（409「不能将自身/后代设为父节点」）；引用/调用允许环（运行时依赖常见）；
6. **深度上限**：树查询/级联删除/父链上溯设递归深度上限（默认 10，可配置），防止意外超深结构拖垮查询；
7. **删除策略**：删除数据项按所属集合的 `delete_policy` 执行（见 §7）；
8. **权限**：见 §8。

---

## 6. 查询设计

### 6.1 API 草案

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/v1/items/:itemId/relations` | 建边 `{type, to_item_id, meta?}` |
| POST | `/api/v1/items/:itemId/relations/batch` | 批量建边 `{relations: [{type, to_item_id, meta?}]}`（Q7 已确认；单条失败不影响其他，返回成功/失败明细） |
| GET | `/api/v1/items/:itemId/relations?direction=out\|in&type=&page=&page_size=` | 出边/入边分页列表（含对端摘要） |
| PATCH | `/api/v1/relations/:relationId` | 修改 meta |
| DELETE | `/api/v1/relations/:relationId` | 删除单条边 |
| GET | `/api/v1/items/:itemId/tree?direction=desc\|asc&depth=&type=parent_child` | 层级树（子树/祖先链） |
| DELETE | `/api/v1/items/:itemId?cascade=&force=&dry_run=` | 数据项删除（策略化，见 §7） |
| GET | `/api/v1/items/:itemId/delete-impact` | 删除预检：返回关联影响清单（弹窗渲染用，等价 dry_run） |

响应统一 `{code, message, data}`；关系列表项格式：

```json
{
  "id": "边id",
  "type": "reference",
  "direction": "out",
  "target": { "item_id": "…", "path": "/nfs/…", "collection_id": "…", "collection_name": "…",
              "summary_tags": {"model_name": "x", "version": "1.2"} },
  "meta": {"alias": "model_v1"},
  "created_at": "ISODate"
}
```

> 对端摘要组装：先按 from/to 索引查边（分页），再按 `_id IN` 批量查数据项取 path/标签（每页 ≤ 200 个 id，两次查询组装，避免 $lookup 扫描）。

### 6.2 树查询（v1：递归 BFS）

```
子树（descendants）：沿 parent_child 出边 BFS，逐层批量查询（IN ids），
                    最多 depth 层（默认 3，上限 10，可配置——Q6 已确认）；
                    每层返回上限 500 条（超出截断并提示，可配置）
祖先链（ascendants）：沿 parent_child 入边逐层上查
```

- 层内排序：按 `meta.layer_order` 升序（缺省按 created_at）；
- 10 万节点/数百层的版图，子树查询 ≈ depth 次 MongoDB 查询（每次毫秒级），可接受；
- 前端树视图懒加载（展开节点时按需查下一层），避免一次拉全树；
- 批量建边（Q7 已确认）：`POST /items/:id/relations/batch`，一次最多 200 条，逐条校验（单父冲突/不存在/自环），返回逐条结果。

### 6.3 物化路径优化（v2，写多读少的版图层级场景）

- 数据项新增冗余字段 `ancestors: [ObjectId...]`（沿父链的全部祖先）；
- **子树查询变成单条查询**：`{ancestors: root_id}`（建 `{ancestors: 1}` 索引）；
- 维护成本：新增父边 → 更新该 item 及其全部后代的 ancestors（按 `{ancestors: item_id}` 批量更新，一条 updateMany）；删除父边 → 对受影响子树重算；
- **权衡**：层级关系写入频繁时维护成本高；版图层级是「建好基本不变、频繁读」的场景，收益明显。v1 不引入，v2 按需开启。

### 6.4 关联数徽标（轻量）

数据查询（DQL）结果行展示 `[子 3] [被引用 2]` 徽标：对当页 item id 批量 `countDocuments`（out/in），一次聚合即可，不进 DQL 主查询。

### 6.5 DQL 关系扩展（Q8 已确认：v2 实施）

- 语法建议：`parent = "item-id"`（直接父子）、`IN children("item-id")`（子树内）等关系函数；
- 或独立查询页「按关联筛选」：选类型 + 目标 item，等价于 `{from_item_id IN (…), type: …}` 预查询后转 item 过滤；
- **v1 不做**，API 层面保持独立，避免 DQL 语法膨胀；v2 按上述语法落地并同步更新 DQL 帮助页。

---

## 7. 连锁删除方案（核心，通用化）

### 7.1 设计取向：通用化（按集合配置删除策略）

不同模块对删除语义需求差异很大：

- **版图库**：层级深，希望「查看引用 → 级联删除整棵子树」；
- **模型库**：被大量用例引用，希望「引用数 > 0 时拒绝删除，先查看引用」；
- **用例库**：用例删除时希望自动解除对 model 的调用关系。

因此删除策略做成**集合级配置 + 统一删除接口**的通用化模型：每个集合可配置 `delete_policy`（默认值全局兜底），
删除数据项时按「数据项所属集合」的策略执行；接口提供 `cascade` / `force` 逃生参数并全程审计。

### 7.2 delete_policy 配置模型

集合文档新增字段（或独立配置文档）：

```json
{
  "delete_policy": {
    "children": "deny",     // 有子节点（parent_child 出边）时：deny | cascade | detach
    "incoming": "deny"      // 被引用/调用（入边数 > 0）时：deny | detach
  }
}
```

| 维度 | 取值 | 行为 |
|---|---|---|
| `children` | `deny` | 有子节点时拒绝删除，返回子节点清单；调用方显式 `cascade=1` 可升级为级联（逃生口） |
| `children` | `cascade` | 自动级联删除整棵子树（弹窗仍需展示子树规模） |
| `children` | `detach` | 自动解除父子边（子节点变孤儿）后删除 |
| `incoming` | `deny` | 入边数 > 0 时拒绝删除，返回引用/调用方清单；显式 `force=1` 可解除后删除（逃生口） |
| `incoming` | `detach` | 自动删除全部入边（解除引用）后删除 |

- 默认值（未配置时）：`children=deny`、`incoming=deny` —— 最安全，与「查看引用，引用数不为 0 才删」的诉求一致；
- 配置权限：该集合的集合管理员（admin 可设全局默认）；
- 逃生参数：`cascade=1`（越过 children=deny 执行级联）、`force=1`（越过 incoming=deny 执行解除），均写入审计。

### 7.3 删除数据项流程（策略化）

```
DELETE /items/:itemId?cascade=&force=&dry_run=
│
├─ 0. dry_run=1（或 GET /items/:itemId/delete-impact）
│     └─ 只返回影响清单，不执行任何删除（前端弹窗渲染用）
├─ 1. 权限校验（item 所在集合：操作工/集合管理员）
├─ 2. 读取集合 delete_policy（无配置用默认）
├─ 3. 收集影响面
│     ├─ 子节点：parent_child 出边（数量 + 路径清单，递归收集子树 ids，深度上限内）
│     └─ 入边：reference/call（数量 + 来源清单）
├─ 4. 策略判定
│     ├─ children=deny 且有子：
│     │     cascade=1 → 级联子树（计入影响清单）
│     │     否则 → 400 拒绝（附子节点清单）
│     ├─ children=cascade 且有子 → 级联子树
│     ├─ children=detach 且有子 → 解除父子边
│     ├─ incoming=deny 且入边数>0：
│     │     force=1 → 解除全部入边（审计）
│     │     否则 → 400 拒绝（附引用/调用方清单）
│     └─ incoming=detach 且入边数>0 → 解除全部入边
├─ 5. 执行（顺序保证引用完整性）
│     ① 删除影响范围内的全部出边（含子树内 item 的边）
│     ② 删除影响范围内的全部入边（解除引用/父关系）
│     ③ 删除受影响的数据项（子树按自底向上批量删）+ 刮削任务（现有逻辑）
├─ 6. 审计：item.delete / item.delete_cascade / relation.delete_bulk
│     （记录子节点数、解除边数、force/cascade 标记）
```

> **顺序说明**：先删边再删数据项，避免「边指向已删除 item」的悬空窗口；
> 若集群 MongoDB 为副本集，可整体放入事务；单节点下窗口极小且查询端对不存在的对端显示「已删除」兜底。

### 7.4 删除集合（扩展现有级联）

```
DELETE /collections/:id
├─ 1. 收集该集合全部数据项 ids（分页遍历）
├─ 2. 删除 data_relations 中 from_item_id ∈ ids 或 to_item_id ∈ ids 的边（批量）
├─ 3. 删除数据项 + 刮削任务（现有逻辑）
└─ 4. 审计（记录解除的跨集合边数——跨集合引用方会因此失去该边）
```

> 跨集合提示：删除集合会**解除其他集合指向本集合数据的边**（入边），删除确认弹窗需展示该影响（§9.4）；
> 集合删除不适用 delete_policy（整集合是管理级操作，仅 admin 可执行，弹窗展示影响后确认）。

### 7.5 删除边

- 删除单条边：直接删，不影响任何数据项；审计 relation.delete；
- 幂等：边不存在返回 404；
- 父子边删除后子节点成为孤儿（无父），可正常存在与查询（树视图显示为根节点）。

### 7.6 并发与一致性

- 唯一索引防重复建边；单父部分唯一索引防父子冲突；
- 删除数据项与并发建边：先删边后删 item，建边方会因「目标不存在」校验失败（存在性检查兜底），不产生悬空边；
- 级联删除与并发：影响清单基于执行时刻快照，执行中的新边以存在性校验兜底，可接受（可选用事务强化）。

---

## 8. 权限设计

| 操作 | 权限要求 |
|---|---|
| 查看关系（出/入边、树） | 数据项所在集合的成员（操作工/集合管理员） |
| 建边 | 起点 item 所在集合：操作工/集合管理员；**且** 目标 item 可访问（目标集合成员或 admin）；跨集合建边需双方均可访问 |
| 改边（meta） | 起点 item 所在集合的操作工/集合管理员（或 admin） |
| 删边 | 边的任一端所在集合的操作工/集合管理员（管理自己数据的关联） |
| 删除数据项（含级联/force） | 数据项所在集合的操作工/集合管理员（沿用现有权限）；`force`/`cascade` 逃生参数不改变角色要求，仅影响策略判定 |
| 配置集合删除策略（delete_policy） | 该集合的集合管理员；admin 可设全局默认值 |

> 集合管理员与操作工对关系的权限一致（关系属于数据操作，不涉及集合配置）；
> admin 全局可见/可操作（沿用「公共权限 + 逐集合判定」模型，集合级权限仍按集合 members 逐集合判定）。

---

## 9. 前端 UX 设计稿

风格延续现有界面：左侧毛玻璃导航、蓝色主调 `#4d6bfe`、卡片悬浮高亮、列表化展示。以下为线框稿。

### 9.1 数据项详情页：新增「关联关系」卡片

```
┌──────────────────────────────────────────────────────────────────┐
│ ← 返回数据查询                            数据项详情                │
│ 基本信息卡片（路径 / 标签来源 / 刮削状态 / 创建信息）                │
│ 标签卡片                                                            │
│ ┌─ 关联关系                                     [+ 添加关联] [树视图]│
│ │  出边（3）                                                       │
│ │  ┌──────────────────────────────────────────────────────────┐  │
│ │  │ 类型            目标                属性          操作     │  │
│ │  │ [父子] 版图层  layer_01             order=1    [跳转][删除] │  │
│ │  │ [调用] 自动化用例 run_case_02       param=…   [跳转][删除] │  │
│ │  │ [引用] model 数据  model_v1         alias=…   [跳转][删除] │  │
│ │  └──────────────────────────────────────────────────────────┘  │
│ │  入边（2）      ← 谁依赖我：变更影响分析                         │
│ │  ┌──────────────────────────────────────────────────────────┐  │
│ │  │ 类型            来源            属性            操作        │  │
│ │  │ [调用] 用例 case_003           -             [跳转][删除]  │  │
│ │  │ [引用] 方案 plan_007           -             [跳转][删除]  │  │
│ │  └──────────────────────────────────────────────────────────┘  │
│ └────────────────────────────────────────────────────────────────┘│
│ 刮削历史卡片                                                        │
└──────────────────────────────────────────────────────────────────┘
交互说明：
- 「添加关联」：操作工/集合管理员可见；打开 §9.2 弹窗
- 「删除」边：confirm 后调 DELETE /relations/:rid，仅删边不影响数据
- 「跳转」：进入目标数据项详情（跨集合可跳转，需有目标集合访问权）
- 「树视图」：父子层级懒加载树，见 §9.3
```

### 9.2 添加关联弹窗

```
┌─ 添加关联 ─────────────────────────────────────────────┐
│ 关联类型                                                │
│   (•) 父子/包含    ( ) 引用    ( ) 调用                 │
│ 目标数据项                                              │
│   [🔍 搜索：输入路径/标签/ID，如 model_name="demo" ] [搜索]│
│   搜索结果（单选，最多展示 20 条）                        │
│   ┌──────────────────────────────────────────────────┐ │
│   │ ○ /nfs/data/models/model_v1        [model 库]    │ │
│   │ ○ /nfs/data/models/model_v2        [model 库]    │ │
│   └──────────────────────────────────────────────────┘ │
│ 关联属性（可选，JSON）                                   │
│   [ {"layer_order": 2, "role": "metal"} ]              │
│   （父子建议提供「层序」快捷输入）                        │
│                              [取消]  [添加]             │
└─────────────────────────────────────────────────────────┘
交互说明：
- 类型切换时提示文案变化（父子=包含层级/引用=弱引用/调用=运行时依赖）
- 搜索复用现有标签查询能力（后端按路径前缀 + 标签等值组合）
- 添加成功 → 详情页出边列表刷新并置顶
```

### 9.3 树视图（版图层级示例，懒加载）

```
┌─ 层级树（父子关系 · 懒加载 · 最多 10 层）────────────────┐
│ ▼ 🗂 版图A /nfs/data/layouts/A      (layout)          │
│    ├─ ▼ layer_01   order=1                            │
│    │     ├─ ▸ cell_001   (单元)                       │
│    │     └─ ▸ cell_002   (单元)                       │
│    ├─ layer_02     order=2                            │
│    └─ layer_03     order=3                            │
│  [展开全部]  [收起]                                     │
└─────────────────────────────────────────────────────────┘
交互说明：
- 节点行：图标（🗂 数据/🔗 引用/⚙ 调用）、名称、order 徽标；点击行 → 跳转该数据项详情
- 展开箭头触发 GET /items/:id/tree?direction=desc&depth=1（仅取下一层）
- 「展开全部」递归拉取（受深度上限保护）；节点含引用/调用子项时以 🔗/⚙ 图标混排展示
- 树内搜索：展开后按名称/路径过滤高亮
```

### 9.4 删除确认（按集合删除策略动态渲染）

```
┌─ 删除确认 ⚠ ───────────────────────────────────────────┐
│ 数据项：/nfs/data/layouts/A                             │
│ 删除策略（集合「版图库」配置）：                         │
│   子节点 = cascade · 被引用 = deny                      │
│                                                        │
│ 关联影响（来自 delete-impact 预检）：                    │
│   • 子节点 3 个（父子）→ 将级联删除                      │
│   • 被引用/调用 2 处（用例 case_003、方案 plan_007）     │
│     → 该集合策略为「拒绝删除」，需勾选强制解除引用        │
│                                                        │
│ 删除方式：                                              │
│   (•) 级联删除全部子节点（3 个）                         │
│   [x] 强制解除外部引用（2 处）→ 删除时自动删除这些边      │
│                                                        │
│ 请输入数据项路径确认：[ /nfs/data/layouts/A        ]     │
│                              [取消]  [确认删除]          │
└─────────────────────────────────────────────────────────┘

策略 deny 时的拒绝提示（未勾选强制）：
┌─ 无法删除 ⚠ ──────────────────────────────────────────┐
│ • 该数据项有 3 个子节点，集合策略不允许直接删除          │
│   （可勾选「级联删除子节点」后重试）                     │
│ • 被 2 处引用/调用，集合策略要求先解除引用               │
│   引用清单：用例 case_003、方案 plan_007  [查看详情]     │
│                              [取消]                     │
└─────────────────────────────────────────────────────────┘
交互说明：
- 打开弹窗前调用 GET /items/:id/delete-impact（dry_run）获取影响清单与策略判定结果
- 弹窗选项由 delete_policy 驱动：children=deny 显示级联勾选；incoming=deny 显示强制解除勾选；
  detach 策略直接提示「将自动解除 N 条边」；cascade 策略直接提示「将级联删除 N 个子节点」
- 路径输入确认防误删（与现有删除逻辑一致）
- 删除集合弹窗同样展示「将解除 N 条来自其他集合的引用」
```

### 9.5 数据查询（DQL）结果行：关联徽标

```
│ 所属集合 │ 路径              │ 标签 │ 关联            │ 来源 │ 状态 │ … │
│ 版图库   │ /nfs/…/layouts/A │ …    │ [子 3] [被引用 2]│ 刮削 │ 已刮削 │ … │
│ 模型库   │ /nfs/…/model_v1  │ …    │ [被调用 5]       │ 刮削 │ 已刮削 │ … │
交互说明：
- 徽标为只读统计（batch count），点击「子 3」→ 跳转该 item 详情并定位关联卡片
- 不进入 DQL 主查询，避免影响查询性能
```

### 9.6 集合管理：删除策略配置卡（集合管理员）

```
┌─ 概览（编辑描述 / 更换管理员 / 删除集合）────────────────┐
│ 删除策略（新增卡片，集合管理员可见）                     │
│   有子节点时：                                           │
│     (•) 拒绝删除（可显式级联）  ( ) 自动级联删除  ( ) 解除父子边 │
│   被引用/调用时：                                        │
│     (•) 拒绝删除（可强制解除）  ( ) 自动解除引用         │
│   [恢复默认]  [保存]                                     │
│   （默认：拒绝删除 / 拒绝删除；改动即时生效，审计记录）    │
└──────────────────────────────────────────────────────────┘
```

### 9.7 导航与入口汇总

| 入口 | 位置 | 说明 |
|---|---|---|
| 关联关系卡片 | 数据项详情页（新增区块） | 出边/入边/添加/树视图 |
| 删除确认弹窗 | 数据项删除、集合删除 | 按集合删除策略动态渲染影响清单与选项 |
| 删除策略配置 | 集合详情页（新增卡片，集合管理员） | children/incoming 策略配置 |
| 关联徽标 | 数据查询结果行 | 只读统计 |
| （v3）关系图谱 | 详情页「图谱」按钮 | 有向图可视化（预留） |

---

## 10. 分阶段实施计划

| 阶段 | 内容 | 验收 |
|---|---|---|
| P1 后端基础 | data_relations 模型 + 索引（含单父部分唯一索引）；建/查/改/删边 + **批量建边** API（含单父校验、父链环检测、meta 校验）；出/入边分页（含对端摘要）；树查询 v1（BFS + 深度/每层上限）；**delete_policy（集合配置 + 默认值）+ 删除预检 delete-impact + 策略化连锁删除（cascade/force/dry_run）**；权限与审计 | 单元测试 + 接口冒烟（建边/防重/自环/单父冲突/环检测/跨集合/批量建边/三种策略下的删除与逃生参数） |
| P2 前端 | 详情页关联卡片（出/入边、添加弹窗（含 layer_order/role 快捷字段）、批量添加、删除边）；树视图（懒加载）；**删除确认弹窗（按策略动态渲染 + 强制解除勾选 + 拒绝提示）**；**集合详情删除策略配置卡**；查询结果关联徽标 | jsdom UI 冒烟测试（建边→列表→树→策略化删除全流程） |
| P3 优化与扩展 | 物化路径（ancestors + 子树单查询）；DQL 关系函数（children() 等，同步更新 DQL 帮助页）；关系统计（仪表盘） | 性能压测（大层级树）、权限矩阵回归 |

---

## 11. 评审结论（Q1~Q8 全部已确认，方案可进入开发）

| # | 结论 |
|---|---|
| Q1 | 父子关系为**单父树**：一个子有且仅有一个父（部分唯一索引兜底）；父链上溯检测环 |
| Q2+Q3 | 删除策略**通用化**：按集合配置 `delete_policy`（children: deny/cascade/detach；incoming: deny/detach，默认均 deny）；配套 `cascade`/`force` 逃生参数、`delete-impact` 删除预检（查看引用/子节点清单）与审计 |
| Q4 | **需要 meta**：任意 JSON；父子类型提供 `layer_order` / `role` 快捷字段（前端表单直填） |
| Q5 | **允许跨集合关系**：权限要求起点集合成员 + 目标可访问 |
| Q6 | 深度上限 10、每层 500，均**可配置**（按初稿） |
| Q7 | **需要批量建边**：`POST /items/:id/relations/batch`（≤200 条/次，逐条校验返回明细），P2 前端提供批量添加入口 |
| Q8 | DQL 关系扩展（children() 等）**v2 实施**：v1 先独立 API，避免语法膨胀 |

---

## 12. 影响面与兼容性

- 现有 API/前端/刮削/DQL 均不破坏（纯增量）：
  - 新增集合 `data_relations`、新增 API、新增审计 action；
  - 数据项删除逻辑扩展（先删边），行为对无关联数据透明；
  - 集合删除逻辑扩展（删边），行为向后兼容；
- `EnsureIndexes` 幂等新增 data_relations 索引，老库直接升级；
- 文档更新：README（关联 API + UX 说明）、需求分解变更记录。
