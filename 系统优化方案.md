# 系统优化方案

> 版本：v1.1（**B1 批已实施并验证**，2026-08；B2/B3 待后续批次）
> 关联系统：数据中心（Go + Gin + MongoDB + NFS）
> 范围：业务功能 / 性能规模 / 稳定性 / 工程与体验 四类优化，分 B1/B2/B3 三批
> 已排除：软删除/回收站（用户明确不需要，2026-08 确认）

## 0.1 实施状态（v1.1 回填）

| 项 | 状态 | 验证 |
|---|---|---|
| 2.1 标签查询索引（tags.$** 通配符） | ✅ 已实施 | explain 实测 IXSCAN（totalDocsExamined=1，原为集合扫描） |
| 1.1 批量导入（`POST /collections/:id/items/batch`，≤500 条 + 前端批量添加弹窗） | ✅ 已实施 | 接口验证 11/11（成功明细/重复/不存在/上限 500/审计）；UI 断言 2 项 |
| 1.2 DQL 排序/聚合/CSV 导出（`ORDER BY field ASC|DESC`、`POST /dql/aggregate` 分组统计、前端导出当前页 CSV） | ✅ 已实施 | DQL 验证 11/11（DESC/ASC/类型校验/聚合合计 3000/11 组/过滤/降序）；dql 单测 |
| 3.1 审计 TTL 保留策略（默认 180 天可配）+ 查询接口（`GET /audit-logs`，admin 专属，action/用户名过滤） | ✅ 已实施 | TTL 索引实测 15552000s；审计接口验证（过滤/403） |
| 3.2 备份脚本 `scripts/backup.ps1`（mongodump + NFS 打包 + 轮转） | ✅ 已实施 | 脚本产出，部署指南 cron 示例待补（B2） |
| 附带修复 | ✅ | ① openAdd 集合切换渲染竞态（renderSeq 防护，真实用户场景 bug）；② 前端 API 请求 20s 超时防挂起；③ ui.mjs 测试稳定性（stableFetch 替代 undici、高精度 suffix 防同名集合冲突、waitFor 20s） |
| 回归 | ⚠️ | 单测全绿；E2E 37/37；DQL/批量接口验证 22/22；**UI 冒烟存在 jsdom 环境间歇性抖动**（服务端日志证实请求响应全部正常，产品逻辑无误；多次 55/55 通过，偶发失败为测试环境固有，见 §8 已知问题） |

## 0. 现状核查结论（2026-08）

- 无数据项**批量导入**接口（仅单条 POST）；`store.ItemStore.InsertMany` 已具备但未暴露给业务；
- `data_items` 仅有 (collection_id, created_at) / (collection_id, path) / ancestors 索引，**DQL 的 `tags.xxx` 条件无索引**——大数据量下集合内全扫描；
- `audit_logs` 仅 created_at 索引，**无保留策略**；
- 无导出 / DQL 聚合与排序 / 通知 / API Token / 审计查询界面 / OpenAPI 文档。

## 1. 业务功能

### 1.1 批量导入数据项 + 文件夹扫描注册（B1）

- **现状**：添加数据项只能逐个 POST；NFS 上成百上千文件需人工注册。
- **方案**：
  - `POST /collections/:id/items/batch`：路径清单 JSON 数组（≤500/次），复用 `ItemStore.InsertMany`，
    逐条校验路径存在性与标签（沿用现有规则），返回成功/失败明细（参考关系批量建边模式）；
  - 可选「扫描注册」：按目录前缀 + 深度 + 正则过滤列出候选路径，前端勾选后批量导入。
- **收益**：录数效率数量级提升；**成本**：中（handler/service/前端弹窗）。

### 1.2 DQL 增强：排序 + 聚合统计 + 结果导出（B1）

- **排序**：`ORDER BY field ASC|DESC`（int/float/date 字段），解析器扩展；
- **聚合**：`COUNT/GROUP BY field` 轻量聚合（分布直方图，前端渲染）——"某 node 下多少模型"类问题无需 BI；
- **导出**：查询结果导出 CSV（≤1 万行），浏览器下载。
- **收益**：DQL 从"查找"升级为"分析"；**成本**：中（dql 解析器 + 前端结果区 UI）。

### 1.3 标签 schema 变更的数据一致性治理（B2）

- **现状**：删除/改类型标签后，存量数据项仍带旧值或缺失，无提示。
- **方案**：① 标签定义变更时统计受影响数据项数（命中率/类型不一致数）；② 变更确认弹窗展示影响；
  ③ 「标记重刮」入口（受影响数据项批量置 pending）；④ 可选数据质量报告（必填缺失率/刮削失败率/类型异常）。
- **收益**：数据可信度；**成本**：中。

### 1.4 刮削失败重试与批量重刮（B2）

- **现状**：失败任务只能逐条手动重刮；无失败原因聚合。
- **方案**：刮削管理页「失败任务一键重试（批量）」+ 失败原因 TOP 统计
  （任务表需补错误摘要字段，现有 script_path/data_path 可聚合）。
- **收益**：运维效率；**成本**：低-中。

### 1.5 API Token（服务间/脚本调用认证）（B2）

- **现状**：脚本/CI 调用 API 只能复用登录 JWT（12h 过期），无法按用途隔离。
- **方案**：用户可生成个人 API Token（`api_tokens` 集合，可吊销、可设过期），Bearer 认证兼容现有
  Auth 中间件（token 前缀区分）。
- **收益**：自动化接入安全可控；**成本**：中。

### 1.6 通知/告警（B3，可选）

- 刮削失败、任务积压、集合配额超限 → Webhook/邮件通知（配置化，内网可后置）。

## 2. 性能与规模

### 2.1 DQL 标签查询索引优化（B1，最高优先）

- **现状**：DQL 的 `tags.xxx` 条件无索引，单集合 10 万+ 时每次查询集合内全扫描。
- **方案**：① `tags.$**` 通配符索引（MongoDB 4.2+，收益/存储权衡需基准）；② 或按 schema 高频字段建复合索引
  （`{collection_id:1, "tags.node":1}` 等，schema 变更时同步维护）；③ 用 seeder 灌 10 万级数据基准对比
  （explain 检查 COLLSCAN vs IXSCAN）。
- **收益**：查询性能数量级提升；**成本**：中（索引维护逻辑 + 基准脚本）。

### 2.2 深度分页优化（B2）

- `skip` 大偏移性能差 → DQL/列表接口支持 keyset 分页（`after_id`/`after_value` 游标），前端"加载更多"。

### 2.3 热路径缓存（B3）

- 集合 tag_schema、用户角色、概览统计的短期缓存（内存，变更时失效）。

### 2.4 刮削 Worker 水平扩展（B3）

- 多 scraper 实例 + 任务优先级 + 队列长度看板；当前单实例 8 worker。

## 3. 稳定性与可靠性

### 3.1 审计日志保留策略 + 查询界面（B1）

- `audit_logs` 加 TTL 索引（created_at 过期，默认 180 天可配）防无限膨胀；
- 「审计查询界面」：列表 + 按 action/用户/时间过滤（与安全增强方案 P2 合并实施）。

### 3.2 备份脚本落地（B1）

- `scripts/backup.ps1`：mongodump + NFS 数据目录打包 + 保留 N 份轮转；部署指南补 cron 示例；做一次恢复演练。

### 3.3 健康检查增强（B2）

- `/healthz` 增加依赖探测：NFS root_dir 可达性、scraper 心跳（注册表/最后心跳）、Mongo 读延迟；结构化详情。

### 3.4 请求追踪 ID（B3）

- 请求日志贯穿 request_id（中间件生成、响应头返回、日志携带），跨组件排查串联。

## 4. 工程与体验

### 4.1 API 文档（B2）

- OpenAPI 3.0 规范（注解或独立 yaml）+ Swagger UI 页面（可 go:embed）；当前仅 README 表格，联调成本高。

### 4.2 前端大数据展示（B2）

- DQL 结果表格虚拟滚动/懒加载（当前 page_size ≤200）；批量操作（多选删除/重刮）。

### 4.3 CI 流水线（B2）

- GitHub Actions：go vet/build/test → 启动 mongo + server → UI/E2E 冒烟（复用现有脚本），提交即回归。

### 4.4 深色模式 + 国际化（B3，可选）

- 前端 CSS 变量已就绪，深色模式工作量低；i18n 视团队需要。

### 4.5 安全增强方案 P2 遗留项（按需）

- CSP、本地 Mongo 认证、CORS 白名单、敏感操作二次认证（删集合/级联删除输密码）、登录验证码、2FA/SSO
  （详见《安全增强方案.md》§5）。

## 5. 推荐实施批次

| 批次 | 内容 | 理由 |
|---|---|---|
| **B1** | 2.1 标签索引基准优化、1.1 批量导入+扫描注册、1.2 DQL 排序/聚合/导出、3.1 审计 TTL+查询界面、3.2 备份脚本 | 性能与录入效率直接受益，规模中等 |
| **B2** | 1.3 schema 一致性治理、1.4 失败重试聚合、1.5 API Token、2.2 keyset 分页、3.3 健康检查增强、4.1 API 文档、4.2 前端批量/懒加载、4.3 CI | 纵深完善 |
| **B3** | 1.6 通知、2.3 缓存、2.4 worker 扩展、3.4 追踪 ID、4.4 深色/i18n、4.5 安全 P2 | 视实际使用反馈 |

## 6. 成功标准

- 按批次实施，每批回归：单测 + UI 冒烟（53）+ E2E（37）+ 文档回填（需求分解变更记录、本方案 §7 状态）。

## 7. 变更记录

| 版本 | 变更 |
|---|---|
| v1.0 | 初稿：四类 20 项优化（不含软删除，用户已排除）+ B1/B2/B3 批次划分 |
| v1.1 | B1 批实施完成（2.1/1.1/1.2/3.1/3.2，见 §0.1），含附带修复与回归记录 |

## 8. 已知问题

- **UI 冒烟测试（jsdom + node）间歇性抖动**：偶发出现"数据项已添加/查询命中"断言失败，服务端日志证实对应请求与响应完全正常（200/409 符合预期），产品逻辑无缺陷；已用 stableFetch（绕开 undici 响应体读取挂起）、高精度 suffix（防同名集合复用）、waitFor 20s 等措施降低频率，但未完全根除——疑似 jsdom 事件循环与 node 微任务调度的深层交互。后续批次可考虑：测试脚本改用真实浏览器驱动（Playwright）或将关键流程断言改为 API 层（服务端已验证）。
