# 部署指南（发布到网上）

将数据中心系统部署到公网/服务器，供团队或外部访问。

## 1. 系统组成与端口

| 组件 | 说明 | 端口 |
|---|---|---|
| `bin/server`（API + Web） | 单二进制，前端已 `go:embed` 内嵌 | 默认 8080（可配） |
| `bin/scraper`（刮削子系统） | 独立进程，消费 MongoDB 任务队列 | 无监听端口 |
| MongoDB | 元数据/标签/权限/任务/关联边 | 27017 |
| Nginx（可选） | 反向代理 + HTTPS | 80/443 |

> 缺省配置即可运行；`scraper` 不启动时系统可用，仅刮削任务不执行。

## 2. 前置条件

- 一台 Linux 服务器（建议 2C4G 起步，取决于数据量；需能访问 MongoDB）
- 域名（可选但推荐，用于 HTTPS）；或直接用服务器 IP
- MongoDB：服务器自装，或云数据库（MongoDB Atlas 等）
- 数据目录：生产环境建议挂载 NFS 共享盘；单机部署可用本地目录

## 3. 方案 A：Linux 服务器直接部署（推荐）

### 3.1 在 Windows 开发机交叉编译 Linux 二进制

```powershell
# 项目根目录（需先执行 .\scripts\goenv.ps1 配置缓存环境）
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o bin/server-linux-amd64 ./cmd/server
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o bin/scraper-linux-amd64 ./cmd/scraper
# 或直接：
make build-linux
```

> `CGO_ENABLED=0` 产出纯静态二进制，不依赖目标机 glibc，可直接运行。
> 也可在 Linux 服务器上安装 Go 后 `go build`（更简单）。

### 3.2 上传并放置

```bash
mkdir -p /opt/datacenter/{bin,config,logs,data}
scp bin/server-linux-amd64 bin/scraper-linux-amd64 user@server:/opt/datacenter/bin/
scp config/config.yaml user@server:/opt/datacenter/config/
# 上传后重命名
mv /opt/datacenter/bin/server-linux-amd64 /opt/datacenter/bin/server
mv /opt/datacenter/bin/scraper-linux-amd64 /opt/datacenter/bin/scraper
chmod +x /opt/datacenter/bin/*
```

### 3.3 生产配置（config/config.yaml）

```yaml
server:
  port: 8080
  mode: release          # 生产必须 release
database:
  uri: mongodb://127.0.0.1:27017/datacenter   # 或带鉴权 mongodb://user:pass@127.0.0.1:27017/datacenter?authSource=admin
  name: datacenter
jwt:
  secret: <openssl rand -hex 32 生成>
  expire_hours: 72
log:
  level: info
  output: stdout          # systemd 下建议 stdout（journal 收集）
data:
  root_dir: /nfs/data     # 生产：NFS 挂载的数据根目录；单机：/opt/datacenter/data
bootstrap:
  admin_username: admin
  admin_password: <首次启动创建后立即修改>
scrape:
  worker_count: 8
  timeout_seconds: 1800
```

> 生产环境务必：`mode: release`、强随机 `jwt.secret`、修改默认管理员密码、
> MongoDB 开启鉴权（见 §6 安全清单）。

### 3.4 systemd 服务

`/etc/systemd/system/datacenter-server.service`：

```ini
[Unit]
Description=Datacenter API Server
After=network.target mongod.service
Wants=mongod.service

[Service]
User=datacenter
Group=datacenter
WorkingDirectory=/opt/datacenter
ExecStart=/opt/datacenter/bin/server -config /opt/datacenter/config/config.yaml
Restart=always
RestartSec=3
Environment=DATACENTER_JWT_SECRET=<与配置一致或留空>

[Install]
WantedBy=multi-user.target
```

`/etc/systemd/system/datacenter-scraper.service`（同上，ExecStart 换成 scraper）：

```ini
ExecStart=/opt/datacenter/bin/scraper -config /opt/datacenter/config/config.yaml
```

启用：

```bash
useradd -r -s /usr/sbin/nologin datacenter
mkdir -p /nfs/data && chown datacenter:datacenter /nfs/data
systemctl daemon-reload
systemctl enable --now datacenter-server datacenter-scraper
systemctl status datacenter-server
```

### 3.5 Nginx 反向代理 + HTTPS

`/etc/nginx/conf.d/datacenter.conf`：

```nginx
server {
    listen 80;
    server_name your.domain.com;
    client_max_body_size 20m;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}
```

```bash
# 申请 HTTPS 证书（有域名时）
apt install -y certbot python3-certbot-nginx
certbot --nginx -d your.domain.com
```

> 无域名时直接用 `http://服务器IP:8080` 访问（需安全组放行 8080）；或配置 IP 自签证书。

### 3.6 防火墙/安全组

- 云服务器安全组：仅放行 80、443（以及调试期需要的 8080）；
- 服务器本地防火墙：`ufw allow 80,443/tcp`；
- **MongoDB 端口 27017 不要暴露公网**（见 §6）。

### 3.7 验证

```bash
curl http://127.0.0.1:8080/healthz
# {"code":0,...}
curl -X POST http://127.0.0.1:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"<新密码>"}'
```

浏览器访问 `https://your.domain.com` 登录即部署完成。

## 4. 方案 B：Docker Compose 一键部署

项目根目录 `docker-compose.yml`：

```yaml
services:
  mongo:
    image: mongo:7
    restart: always
    volumes:
      - mongo-data:/data/db
    networks: [datacenter]

  server:
    build:
      context: .
      dockerfile: Dockerfile.server
    restart: always
    environment:
      DATACENTER_SERVER_MODE: release
      DATACENTER_DATABASE_URI: mongodb://mongo:27017
      DATACENTER_DATABASE_NAME: datacenter
      DATACENTER_JWT_SECRET: ${DATACENTER_JWT_SECRET:?set it}
      DATACENTER_BOOTSTRAP_ADMIN_PASSWORD: ${DATACENTER_ADMIN_PASSWORD:?set it}
      DATACENTER_DATA_ROOT_DIR: /nfs/data
    volumes:
      - /nfs/data:/nfs/data
    ports:
      - "8080:8080"
    depends_on: [mongo]
    networks: [datacenter]

  scraper:
    build:
      context: .
      dockerfile: Dockerfile.scraper
    restart: always
    environment:
      DATACENTER_DATABASE_URI: mongodb://mongo:27017
      DATACENTER_DATABASE_NAME: datacenter
      DATACENTER_SCRAPE_POLL_INTERVAL_MS: "2000"
    volumes:
      - /nfs/data:/nfs/data
    depends_on: [mongo]
    networks: [datacenter]

volumes:
  mongo-data:

networks:
  datacenter:
```

`Dockerfile.server`：

```dockerfile
FROM golang:1.25 AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /out/server ./cmd/server

FROM alpine:3.20
RUN apk add --no-cache ca-certificates
COPY --from=build /out/server /usr/local/bin/server
EXPOSE 8080
ENTRYPOINT ["server", "-config", "/etc/datacenter/config.yaml"]
```

> `Dockerfile.scraper` 同理（入口换成 scraper）；`config.yaml` 需 COPY 进镜像或挂载。
> 注意：刮削脚本执行在容器内，脚本与 NFS 数据都需挂载进容器（`/nfs/data` 等）。

```bash
docker compose up -d
docker compose ps
```

## 5. 方案 C：局域网快速上线（测试用）

1. Windows 开发机直接启动：`go run ./cmd/server -config config/config.yaml`（或 `make run`）；
2. 修改 `config.yaml` 的 `jwt.secret` 与 bootstrap 密码；
3. Windows 防火墙放行 8080：`netsh advfirewall firewall add rule name="datacenter" dir=in action=allow protocol=TCP localport=8080`；
4. 同事访问 `http://<你的内网IP>:8080`。

> 仅适合内网测试；公网暴露请务必先做 §6 安全清单。

## 6. 安全清单（上线前必做）

| 项目 | 操作 |
|---|---|
| 默认密码 | 首次启动后立即修改 admin 密码（个人设置页），或启动前用 `DATACENTER_BOOTSTRAP_ADMIN_PASSWORD` 覆盖 |
| JWT 密钥 | `openssl rand -hex 32` 生成，用环境变量 `DATACENTER_JWT_SECRET` 注入，勿提交到仓库 |
| 运行模式 | `server.mode: release`；**v0.16 起 release 模式会校验默认 JWT secret / 默认管理员口令并拒绝启动**（防止裸奔），请务必先覆盖再启动 |
| 密码策略 | v0.16 起统一要求 **≥8 位且包含字母与数字**（创建用户 / 重置 / 个人改密） |
| 登录防爆破 | v0.16 起同一 IP+用户名连续 5 次失败锁定 15 分钟（内存级，单实例）；反向代理部署时注意 `ClientIP` 取直连地址（TrustedProxies 需按拓扑配置） |
| MongoDB 鉴权 | 开启 `--auth`，创建独立账号，URI 带用户名密码；**27017 不暴露公网**（bindIp 127.0.0.1 或仅内网） |
| HTTPS | 有域名用 certbot；否则至少保证数据经可信网络传输 |
| 数据目录权限 | `data.root_dir` 归属服务账号，禁止 777；脚本目录同理（刮削脚本在 NFS 上，需可执行） |
| 服务账号 | 用非 root 用户运行（systemd `User=datacenter`；Docker 内非 root） |
| 刮削脚本 | 脚本由集合管理员维护，执行器为普通子进程（Q12）；部署环境确认 NFS 上脚本具可执行位 |
| 备份 | MongoDB 定期 `mongodump`；NFS 数据按组织策略备份 |

### 6.1 JWT secret 轮换流程（v0.16 补充）

1. 生成新密钥：`openssl rand -hex 32`；
2. 修改环境变量 `DATACENTER_JWT_SECRET` 并重启 server；
3. **所有已签发 token 立即失效**（签名密钥变化），全部用户需重新登录——建议在维护窗口执行并提前通知；
4. 如需保留部分用户会话，可配合 v0.16 的 `password_version` 机制：不改密钥、仅对目标用户重置密码（其旧 token 立即失效），普通轮换仍建议整体重启。

> 注意：v0.16 起 JWT 默认有效期收紧为 **12 小时**（`jwt.expire_hours` 可配置，内网可放宽）。

## 7. 常见问题

| 问题 | 处理 |
|---|---|
| Windows 编出的二进制在 Linux 报错 | 用 `CGO_ENABLED=0 GOOS=linux GOARCH=amd64` 交叉编译（make build-linux） |
| 数据路径不存在报「路径不存在或不可访问」 | 确认服务器上 `data.root_dir` 目录存在且服务账号可读；NFS 需先挂载 |
| 刮削脚本执行失败 | Linux 上脚本需要可执行位（`chmod +x`）；确认 shebang（如 `#!/usr/bin/env python3`） |
| 中文乱码/时区 | 系统 `TZ=Asia/Shanghai`；MongoDB 时间统一 UTC 存储，前端按本地展示 |
| 日志轮转 | systemd 下用 journal（`journalctl -u datacenter-server`）；文件输出用 logrotate |
| 改了前端没生效 | 前端内嵌在二进制，需重新编译并重启 server（浏览器硬刷新 Ctrl+F5） |
| 端口被占用 | 改 `server.port` 或 nginx 转发端口 |

## 8. 快速检查清单

- [ ] `mode: release`、强 JWT secret、默认密码已改
- [ ] MongoDB 鉴权开启、27017 未暴露公网
- [ ] `data.root_dir` 存在且权限正确
- [ ] server + scraper 均在运行（systemctl status）
- [ ] `curl /healthz` 返回 code 0
- [ ] 浏览器可登录、能建集合/查数据/触发刮削
- [ ] HTTPS 证书有效（如有域名）
